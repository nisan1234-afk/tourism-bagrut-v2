# AI Shared Worklog

יומן התיאום המשותף של Codex ו-Claude עבור `tourism-bagrut-v2`.

## לפני כל עבודה

> לפני בניית יחידה חדשה יש לקרוא גם את [`docs/UNIT_EXPANSION_PLAYBOOK.md`](docs/UNIT_EXPANSION_PLAYBOOK.md).

1. לקרוא את הקובץ הזה ואת המסמכים בתיקיית `docs`.
2. לבדוק את `main` ואת היסטוריית השינויים.
3. לבדוק אם הסוכן האחר כבר ביצע או שינה את המשימה.
4. לא לשנות את `tourism11`, `jerusalem-tour` או כיתה פלוס ללא הוראה מפורשת.
5. לא לשמור סודות, tokens, מפתחות API או מידע אישי.

## אחרי כל עבודה

להוסיף בראש היומן רשומה עם: תאריך, מבצע, בקשה, שינויים, קבצים, בדיקות, commit, push/deploy ומה נותר פתוח.

## יומן

### 2026-08-27 — Claude Code — מסמך "פרומפט מוכן" להוספת חבל חדש בוואן-שוט

- **בקשה:** אחרי שהאתר הוכרז "מוכן לעבודה" (ר' רשומה למטה) — לא לבנות
  עוד באתר עכשיו, אלא להכין מסמך/פרומפט כך שבפעם הבאה שהמשתמש מביא
  תיקיית חומר על חבל ארץ חדש, סשן עתידי (בלי זיכרון מהסשן הזה) יוכל
  לבנות את היחידה בוואן-שוט בלי לפספס משהו מהתקן הקיים.
- **בוצע:** `docs/NEW_REGION_ONE_SHOT_PROMPT.md` — מסמך בפורמט "פרומפט
  להעתקה" (לא רק הסבר): סעיף א' עם שדות למילוי (שם חבל, unit_id, לינק
  דרייב, סטטוס אישור תמונות/בגרות), סעיף ב' עם הוראות בנייה מלאות
  (סדר קריאה — CLAUDE.md → הצ'קליסטים → `valleys-unit.js` כתבנית-ייחוס
  נקייה; כלל "לחלץ לא להמציא"; 10 צעדי בנייה כולל אזהרה מפורשת על באג
  ה-`window.API` שקרה פעמיים; דרישות תיעוד; commit+push), וסעיף ג' עם
  גבולות מפורשים — מה הפרומפט **לא** מכסה (רישום unit_id בגיליון, העלאת
  תמונות ל-jerusalem-tour, בדיקה חיה בדפדפן) וסדר מומלץ אחרי הוואן-שוט.
- **תיקון נלווה:** `docs/UNIT_EXPANSION_PLAYBOOK.md` §12 עדכן — סעיף 4
  ("רישום ב-BAGRUT_UNITS דורש סבב B") היה מיושן מאז ש-BAGRUT_UNITS הפך
  דינמי (25.08); עודכן לשקף שאין יותר תלות ב-B לזה.
- **תיעוד:** `CLAUDE.md` — מפת המסמכים כוללת עכשיו קישור למסמך החדש.
- **פתוח:** המסמך לא נבדק עדיין בהרצה אמיתית (חבל חדש הבא יהיה המבחן).

### 2026-08-27 — Claude Code (הכנת פרומפט) + B (ביצוע בדפדפן, live) — סבב QA שני: באג אמיתי בווידג'ט המאמן ב-3 יחידות + האתר הוכרז מוכן לעבודה (תוכן בלבד חסר)

- **בקשה:** סבב QA שני שסוגר את הפערים מהסבב הראשון — שער כניסה בכל 5
  יחידות, המאמן ב-4 יחידות שלא נבדקו, ייבוא CSV קצה-לקצה.
- **נמצא ותוקן ע"י B (חמור, לא קוסמטי):** ווידג'ט המאמן היה **שבור
  לגמרי** ב-`galilee-unit.js`/`valleys-unit.js`/`dead-sea-unit.js` —
  `ReferenceError: API is not defined` בכל שליחת שאלה. הסיבה: כתובת ה-
  API הוגדרה כ-`const` בתוך סגירת IIFE, אבל `callBagrutBot` (שנוסף
  מאוחר יותר, ר' רשומות 25.08) הוגדרה **מחוץ** לסגירה ולא ראתה את
  המשתנה. ירושלים (`region-unit.js`) לא נפגעה — שם המשתנה בשם אחר
  (`regionApiUrl`) ולא הייתה בעיית scope. **תוקן:** `window.API=API`
  בתוך הסגירה, ועדכון הקריאות ל-`window.API`, בשלושת הקבצים. `node
  --check` עבר על כל השלושה. אומת live בפועל: שאלה אמיתית → תשובה
  אמיתית → חלון לא נשבר, בכל אחת מ-4 היחידות (כולל ירושלים ללא שינוי).
- **ייבוא CSV — ✅ אומת קצה-לקצה live לראשונה:** הורדת תבנית → מילוי
  שורה עם דוא"ל תקין → שמירה → העלאה חזרה → "הרשימה נוספה בהצלחה",
  מספר התלמידים עלה מ-3 ל-4 והרשומה מופיעה נכון בטבלה.
  **הערה לניקוי:** נוצר תלמיד-בדיקה אמיתי בגיליון
  (`qa.test.student@example.com`, "תלמיד בדיקה QA") — B לא הצליח להסיר
  אותו (כפתור ההסרה תלוי ב-`confirm()` דפדפן שתקע את האוטומציה). **יש
  להסיר ידנית** בטאב "תלמידים וכיתות" בדשבורד המורה.
- **שער כניסה — ✅ אומת:** כל 6 הכתובות (index + 5 יחידות) בלי התחברות
  מציגות רק את מסך "האתר דורש כניסה"; עם התחברות תקינה נטענות כרגיל.
  אומת גם מבנית שסקריפט ההפניה יושב ב-`<head>` לפני `<body>` בכל 6 —
  אין הבזק תוכן לפני ההפניה.
- **cache-busting:** גרסאות `?v=` עודכנו ב-3 קבצי ה-HTML של היחידות
  שתוקנו. הבקאנד (`bagrut.gs`/`code.gs`) לא נערך בסבב הזה — עדיין 66.
- **הכרזה:** לפי B ולפי המשתמש — "האתר מוכן לעבודה, חוץ מזה שצריך
  להוסיף חבלים וחומר". הפער היחיד שנשאר הוא **תוכן** (עוד חבלים,
  תמונות לגליל), לא קוד/תשתית.
- **תיעוד:** `CLAUDE.md` סעיפים 6-7 ברשימת הסיכונים יעודכנו בהתאם
  (verification debt שהיה תיאורטי — עכשיו נסגר ברובו לאחר הסבב הזה).
- **פתוח:** להסיר את תלמיד-הבדיקה מהגיליון (ידני, לא קוד).

### 2026-08-26 — Claude Code (הכנת פרומפט) + B (ביצוע בדפדפן, live) — סבב בדיקה חי: 2 באגים אמיתיים נמצאו ותוקנו

- **בקשה:** לפני עוד פיצ'רים, סבב בדיקה חי אחד באתר (לא רק תחביר) — שרשרת
  שלמה של שינויים (coach widget, שער כניסה, ייבוא CSV, 05-08) נבנתה
  מסשנים בלי גישת דפדפן, ולא אומתה חזותית אף פעם.
- **נשלח ל-B:** פרומפט של 6 בדיקות חיות (שער כניסה בכל 5 יחידות, המאמן
  בכל יחידה, ייבוא CSV קצה-לקצה, עריכת תוכן, "מה שכחתי"/זיהוי אתרים) עם
  הנחיה לתקן ולפרוס בעצמו רק אם התיקון קטן וברור.
- **נמצא ותוקן ע"י B (מאומת live, לא רק בקוד):**
  - **ווידג'ט המאמן "מתפוצץ" מעבר לגבולות שלו** — חסר `display:flex`
    אפקטיבי + `min-height` שגוי על `.coach-log` מנעו גלילה פנימית תקינה
    של הצ'אט. תוקן ב-`styles.css`: `.coach-widget{display:flex!important}`
    ו-`.coach-log{min-height:0}` (היה `110px`). מאומת live עם נכסים
    טריים בדפדפן.
  - **`saveSiteKnown` לא נשלח בכלל** בלחיצה על תשובה בחידון זיהוי-אתרים
    (מישור החוף) — התשובה סומנה נכון/לא-נכון על המסך, אבל שום דבר לא
    נשמר לשרת. נוספה קריאת `bagrutAPI('saveSiteKnown',{site,region,known})`
    ל-`renderRecognition()` ב-`unit.js`. מאומת קצה-לקצה live: לחיצה →
    קריאת רשת אמיתית → `getMySiteRecognition` חוזר עם הערך שנשמר.
- **נבדק ואומת ללא באג (false negative):** עריכת תוכן דרך `teacher.html`
  נכשלה בבדיקה ראשונה עם "אימות נכשל" — התברר שזו תפוגת token רגילה של
  גוגל (כשעה), לא באג. אחרי התחברות מחדש: `saveContentOverride` → רענון
  → הטקסט מתעדכן, עובד כצפוי.
- **ממצא scope (לא באג):** חידון "זיהוי אתרים" ומסך "מה שכחתי" קיימים
  רק במישור החוף — ביתר 4 היחידות התוכן/המסך פשוט לא נבנו עדיין. תועד
  ב-`CLAUDE.md` כדי שלא ייחשב בטעות לבאג בעתיד.
- **cache-busting:** מספרי `?v=` עודכנו בקישורי CSS/JS בכל 7 קבצי ה-HTML
  (כדי שהתיקונים יגיעו בפועל למשתמשים שכבר טענו גרסאות ישנות). כל השינויים
  committed ישירות ל-`main` דרך GitHub (לא PR). `bagrut.gs`/`code.gs` לא
  נערכו כלל בסבב הזה — עדיין גרסה 66.
- **לא נבדק בכוונה:** ייבוא CSV (מה-25.08) — המשתמש ביקש מ-B במפורש לא
  לגעת בזה בסבב הזה. עדיין לא אומת בדפדפן חי.
- **תיעוד:** `CLAUDE.md` — נוספו סעיפים 6-7 לרשימת הסיכונים: scope-gaps
  של זיהוי-אתרים/מה-שכחתי, וחוב אימות-הדפדפן (עם ההוכחה שהוא ממשי).
- **פתוח:** בדיקה חיה של ייבוא ה-CSV.

### 2026-08-26 — Claude Code (הכנת פרומפט) + B (ביצוע בדפדפן) — בקאנד: 05+06+07+08 אומתו ונפרסו (גרסה 66)

- **בקשה:** לוודא בפועל אם המסמכים 05 (ייבוא מרוכז)/06 ("מה שכחתי")/07
  (זיהוי אתרים)/08 (משוב עמוד/לוג שגיאות) באמת בוצעו בבקאנד — המשתמש לא
  היה בטוח ("נראה לי שכן"), ולסשן הזה אין גישת דפדפן ואין גישת רשת ל-
  `script.google.com` (חסום ברמת מדיניות הסשן, 403) כדי לבדוק ישירות.
- **נשלח ל-B:** פרומפט בדיקה-ואז-פריסה — לחפש 5 שמות פעולה מייצגים בקוד,
  ואם חסר משהו לבצע 05-08 מהתיקייה בדרייב ולפרוס גרסה חדשה.
- **ממצא של B:** חלק מ-05+06 כבר היה קיים ב-`bagrut.gs` (הפונקציות עצמן),
  אבל **החיבור ל-`code.gs`** (`protectedActions`/`handlers`) **היה חסר
  לגמרי**, ו-**07+08 היו חסרים לגמרי בשני הקבצים**. כלומר הפרומפט
  המשולב הקודם לא בוצע במלואו כשהמשתמש חשב שכן.
- **בוצע ע"י B:** הושלמו 5 הפונקציות של 07 ו-5 של 08 ב-`bagrut.gs` (כעת
  744 שורות); נוספו 8 פעולות ל-`protectedActions` ו-10 שורות handlers
  ב-`code.gs` (כולל `submitPageFeedback`/`logClientError` שהושארו
  בכוונה **מחוץ** ל-`protectedActions` — פתוחות ללא login, כפי שתוכנן).
  B אימת מול השרת (בטאב נפרד) שהשמירה בפועל הצליחה לפני הפריסה.
  פורסמה **גרסה 66** לאותו deployment קיים (לא נוצר deployment חדש).
- **תיעוד:** `CLAUDE.md` עודכן — כל 8 הפעולות עברו מ"עדיין לא פרוסות"
  ל"פרוסות וחיות".
- **לקח לתיעוד עתידי:** "המשתמש אמר שביצע" אינו הוכחה מספקת שפעולה
  אכן נפרסה במלואה — כדאי לבקש מ-B לבדוק/לאמת בפועל (לא רק לבצע) גם
  כשההנחה היא שזה כבר בוצע.
- **פתוח:** לבדוק בפועל באתר החי: ייבוא רשימה מרוכז (כולל תכונת ה-CSV
  החדשה מהרשומה הבאה למטה), "מה שכחתי", זיהוי אתרים, משוב עמוד.

### 2026-08-26 — Claude Code — ייבוא רשימת תלמידים דרך קובץ CSV/אקסל במקום הדבקת טקסט

- **בקשה:** "אני רוצה שנעשה את זה אחרת - הוספת רשימה, זה יהיה קובץ אקסל
  שירד לי למחשב, ואני אמלא בו את הפרטים, פה תבנית, ואחזיר חזרה" — במקום
  הדבקת שורות טקסט חופשי בטופס "ייבוא רשימה" בדשבורד המורה.
- **הוחלט (נשאל המשתמש):** CSV אמיתי עם BOM (נפתח ישירות באקסל, נשמר
  כ-CSV בלחיצת Ctrl+S) ולא .xlsx אמיתי — כדי לא להוסיף תלות בספריית CDN
  חיצונית (SheetJS) ולהישאר תואם למוסכמות הקוד הקיימות (JS וניל, בלי
  ספריות). לפי חוויית המשתמש בפועל זה עדיין "קובץ אקסל" לכל דבר.
- **בוצע:**
  - `teacher.html`: טופס "ייבוא רשימה" — הוסרה ה-textarea, נוסף כפתור
    "⬇ הורדת תבנית (CSV לאקסל)" ושדה `<input type="file" accept=".csv">`.
  - `teacher.js`: פונקציית `downloadCSV()` גנרית (בונה Blob עם BOM
    בתחילת הקובץ כדי שהעברית תיפתח נכון באקסל, מוריד דרך `<a download>`
    זמני); כפתור התבנית מוריד `תבנית_ייבוא_תלמידים.csv` עם שורת כותרות
    (שם/דוא"ל/כיתה) ושורת דוגמה. handler ה-submit הוחלף: קורא את הקובץ
    שנבחר (`file.text()`), מסיר BOM מוביל, מפרק לשורות ולעמודות (אותו
    פורמט `split(/[,\t]/)` כמו קודם), ומסנן שורות לפי `email.includes('@')`
    — זה גם מדלג אוטומטית על שורת הכותרות (ל-"דוא\"ל" אין @) בלי צורך
    לזהות אותה במפורש. הקריאה ל-`addBagrutStudentsBulk` נשארה זהה.
- **בדיקות:** `node --check teacher.js` עבר. גרסת cache עודכנה
  ל-`teacher.js?v=7`. לא בוצעה בדיקה חיה בדפדפן (אין גישת דפדפן מהסשן).
  מומלץ לבדוק בפועל: הורדת התבנית, מילוי שורה באקסל אמיתי, שמירה
  ב-Ctrl+S, והעלאה חזרה — לוודא שהעברית לא מתקלקלת ושהתלמיד נוסף.
- **פתוח:** אין תמיכה בקובצי `.xlsx` אמיתיים (רק CSV) — אם בעתיד ירצה
  המשתמש תמיכה מלאה ב-.xlsx, זה ידרוש ספריית CDN חיצונית (SheetJS),
  החלטה שנדונה ונדחתה במפורש בשלב הזה.

### 2026-08-25 — Claude Code (הכנת פרומפט) + B (ביצוע בדפדפן) — בקאנד: `BAGRUT_UNITS` הפך לדינמי

- **בקשה:** המשך "יישור קו" — הפריט האחרון שהמשתמש אישר להוסיף לפרומפט
  הבא ל-B (מתוך שאלת "מה נותר לעבודה עתידית").
- **בעיה:** `BAGRUT_UNITS` היה מערך קבוע בקוד ב-`bagrut.gs`; רישום יחידה
  חדשה (כמו שקרה עם "הגליל" לפני מסמך 03) חייב תמיד סבב B נפרד.
- **נשלח ל-B (additive-only, בלי גישה לקובץ המלא):** `ensureBagrutUnitsSheet_`
  (יוצר טאב `bagrut_units` אם חסר, זורע אותו מהמערך הקבוע בפעם הראשונה),
  `getBagrutUnits_()` (קורא מהטאב; אם ריק — נופל בחזרה למערך המקורי, שנשאר
  בקוד ללא שינוי), פעולה מוגנת חדשה `addBagrutUnit` (מאמתת פורמט `unit_id`).
  הפרומפט כלל הנחיה מפורשת ל-B לאתר בעצמו את כל מקומות השימוש הקיימים
  ב-`BAGRUT_UNITS` ולהחליף רק את ה-*reference*, לא את ה-declaration — כי
  לסשן הזה אין גישת דפדפן/גישה לקובץ המלא.
- **בוצע:** המשתמש אישר "בוצע" (Manage deployments → New version).
- **קבצים:** `bagrut.gs`/`code.gs` (בריפו/פרויקט Apps Script, לא ב-git של
  `tourism-bagrut-v2`).
- **בדיקות:** לא בוצעה בדיקה חיה מהסשן הזה (אין גישת דפדפן/Apps Script).
  מומלץ לוודא בפעם הבאה שיש גישה: לרשום יחידה חדשה דרך `addBagrutUnit`
  ולוודא שהיא מופיעה בדשבורד המורה בלי redeploy.
- **תיעוד:** `CLAUDE.md` עודכן — סעיף "BAGRUT_UNITS" תחת "בקאנד" וטבלת
  הפעולות הפרוסות.
- **פתוח:** מסמכים 05+06+07+08 (ייבוא מרוכז, "מה שכחתי", זיהוי אתרים,
  משוב עמוד) — המשתמש דיווח ששלח את הפרומפט המשולב ל-B אבל טרם אישר ביצוע
  ("בוצע"); לוודא/לתעד בפעם הבאה שיש עדכון.

### 2026-08-25 — Claude Code — הסרת מצב-אורח לגמרי + ניקוי קוד מת

- **בקשה:** "פשוט להסיר לגמרי - לחייב התחברות לכל האתר" (החלטה נפרדת
  שהתבקשה תוך כדי דיון "יישור קו").
- **בוצע (commit `69951bd`):** נוסף gate כושל-בטוח בטרם-רינדור לכל האתר —
  אותו דפוס בדיוק כמו `teacher.html` הקיים: סקריפט ב-`<head>` בודק
  `sessionStorage.kitahUser` באופן סינכרוני ומוסיף `page-authorized` ל-
  `<html>`; `body` מקבל `class="protected-page"` שמסתיר הכול חוץ מ-
  `.access-gate` (מסך "האתר דורש כניסה" עם קישור לכיתה פלוס) עד שהמחלקה
  מתווספת. הוחל על `index.html` וכל 5 דפי `units/*.html`.
- **בוצע (commit `6fe5573`, ניקוי המשך):** הוסר קוד מת שקישר ל-guest-mode
  הישן שכבר לא נגיש: דיאלוג `#loginDialog` (ב-`index.html`,
  `coastal-plain.html`, `jerusalem.html`), בלוק "guest-note" בסיידבר,
  כפתור "כניסה" מת בטופבר, פסקת `login-hint` שגויה עובדתית (טענה שיש מצב
  אורח), כל 7 מאפייני `data-login-feature="..."` (האלמנטים עצמם — כמו
  קישורי ניווט "נעול" — נשארו כפי שהם, בכנות משקפים פיצ'רים שעדיין לא
  נבנו). כפתור "שמירת התקדמות" הלא-פונקציונלי הוסר לגמרי ממישור החוף.
  ה-JS התואם ב-`app.js` הוסר (קונסטים `loginDialog`/`dialogText`/
  `KITA_PLUS_URL`, כל ה-click-listener שהציג את הדיאלוג).
- **לא הוסר במכוון:** כללי CSS מתים (`.login-dialog`/`.guest-note`) —
  ניקוי בסיכון נמוך/תועלת נמוכה, לא נחוץ.
- **בדיקות:** `node --check` על כל קובץ JS ששונה. בדיקה חיה בדפדפן לא
  בוצעה מהסשן הזה (אין גישת דפדפן).
- **תיעוד:** `CLAUDE.md` עודכן — סעיף 5 ברשימת הסיכונים (guest-mode) הוחלף
  מ"טרם הוחלט" ל"הוסר לגמרי", וטבלת "מצב לפי יחידה" קיבלה הערה שכל
  היחידות זהות מבחינת דרישת התחברות.
- **פתוח:** אין — ההחלטה בוצעה במלואה כפי שהתבקשה.

### 2026-08-25 — Claude Code — עיצוב פורמט-נתונים למשחקים; החלטה מודעת לא לחלץ את מישור החוף בעיוורון

- **בקשה:** המשך "יישור קו" עם סמכות מלאה מהמשתמש לפעול לפי שיקול דעת
  ("אם זה הדבר הנכון תעשה את זה... כל שאלה תשאל את עצמך מה הייתי רוצה").
- **בוצע:** נקרא `unit.js` במלואו (268 שורות/86KB, כל 10 המשחקים + דאטה +
  לוגיקה מעורבבים באותם משתנים). זוהה שכל 10 המשחקים מתכנסים ל-7 "מנועי
  משחק" גנריים (`select-match`, `multi-choice-sequence`, `ordered-select`,
  `memory-pairs`, `image-puzzle`, `map-points`, `streak-quiz`) — תועד
  ב-`CLAUDE.md` כפורמט-הנתונים הרשמי למשחקים עתידיים.
- **הוחלט במודע לא לבצע:** חילוץ בפועל של מישור החוף לפורמט הזה. הסיבה:
  זה פיצ'ר עשיר שכבר עובד לתלמידים אמיתיים, אין גישת דפדפן לבדוק ויזואלית
  שכלום לא נשבר, והמשתמש לא זמין כרגע לבדוק בעצמו. זו החלטת-סיכון מודעת,
  לא דחיינות — תועדה במפורש ב-`CLAUDE.md` כדי שסשן עתידי לא ינסה לבצע את
  זה "בעיוורון" בלי לקרוא את ההערה הזו קודם.
- **פתוח:** חילוץ מישור החוף בפועל — ממתין לגישת דפדפן (B) או לעבודה
  הדרגתית עם אימות אנושי אחרי כל שלב.

### 2026-08-25 — Claude Code — ניקוי תיעוד: CLAUDE.md חדש, תיקון מסמך ארכיטקטורה, תוכנית ארוכת-טווח

- **בקשה:** דיון (לא בקוד) על שאלות אסטרטגיות — פרונט חדש+בק ישן מול עיצוב
  מחדש של הישן; האם להתחיל נקי לגמרי; האם יש מסמכים מבלבלים/סותרים; בקשה
  לתוכנית ארוכת-טווח שמתחשבת בחבלים עתידיים, תמונות עתידיות, ונושאים
  לא-גיאוגרפיים (שיווק וכו').
- **ממצא מרכזי:** בדיקת `UNIT_EXPANSION_PLAYBOOK.md` §9 העלתה שההחלטה
  שכבר התקבלה שם ב-23.08 ("לא לשכפל את מישור החוף לפני חילוץ מודל-נתונים
  מלא") **נעקפה בשקט** למחרת — העבודה בפועל הלכה לכיוון קל יותר (קובץ-
  התנהגות משותף, בלי חילוץ למודל נתונים). כמו כן: `docs/ARCHITECTURE.md`
  הציג "מודל תוכן מתוכנן" (`course`/`unit`/`section`/...) כאילו הוא בתוקף,
  כשבפועל הוא מעולם לא נבנה — מטעה לסשן עתידי. וגם: לריפו הזה **אין**
  `CLAUDE.md` בכלל (בניגוד ל-tourism11), אז אין מקור-אמת יחיד ל"מה המצב
  עכשיו" — מפוזר על פני README + 6 קבצי docs + AI_WORKLOG בן 63KB.
- **בוצע (תיעוד בלבד, אין שינוי קוד):**
  - **`CLAUDE.md` חדש** בשורש הריפו — מקור-אמת יחיד למצב הנוכחי: מפת
    מסמכים (מה לקרוא ומתי), החלטת הארכיטקטורה (לאשרר את הדפוס הקל, לא
    P0 הכבד), התקן המחייב ליחידה חדשה, טבלת מצב לפי יחידה (5 יחידות,
    דפוס קוד, פערים), רשימת פעולות בקאנד פרוסות מול ממתינות, וסיכונים
    ידועים (מיספור data-field-key שביר, אין טסטים, תלות ב-B, וכו').
  - **`docs/ARCHITECTURE.md`**: סעיף "מודל תוכן מתוכנן" סומן במפורש
    כהיסטורי/לא-בוצע, עם הפניה ל-CLAUDE.md.
  - **`docs/UNIT_EXPANSION_PLAYBOOK.md`**: נוספו סעיפים 11 (עדכון ההחלטה),
    12 (תוכנית ארוכת-טווח — חבל חדש/תמונות חדשות/נושאים לא-גיאוגרפיים),
    13 (סיכונים ידועים).
- **פתוח:** מישור החוף עדיין לא אוחד (הפער הגדול ביותר שנשאר); החלטה
  לגבי צמצום/הסרת מצב-אורח טרם התקבלה סופית; `BAGRUT_UNITS` עדיין מערך
  קבוע בקוד (לא טאב גיליון דינמי).

### 2026-08-25 — Claude Code — "המאמן שלי לבגרות" בכל 5 היחידות

- **בקשה:** להרחיב את ווידג'ט המאמן (שנבנה למישור החוף ברשומה הקודמת)
  לירושלים/עמקים/ים המלח/הגליל.
- **בוצע:** אותן 4 פונקציות (`callBagrutBot`/`toggleCoachWidget`/
  `appendCoachMsg`/`sendCoachQuestion`) נוספו לסוף כל אחד מארבעת קובצי
  ה-JS הייעודיים (`region-unit.js`, `valleys-unit.js`, `dead-sea-unit.js`,
  `galilee-unit.js`), כל אחד קורא לשם משתנה ה-API המקומי שלו (`regionApiUrl`
  או `API`). בכל HTML נוסף רק הכפתור הצף + ווידג'ט הצ'אט (בלי כפתור בכותרת
  כמו במישור החוף, כדי לא לגעת במבנה ה-topbar השונה של כל יחידה) עם כותרת
  ייעודית ("המאמן שלי — ירושלים" וכו').
  שוב, אין צורך בפריסת Apps Script — `askBagrutBot` פתוח וכבר חי.
- **נבדק:** `node --check` על 4 קובצי ה-JS. **לא בוצעה בדיקה חזותית.**
- **פתוח:** מיקום הכפתור הצף (bottom-left, מעל `.lesson-footer`) חושב לפי
  גובה קבוע, לא נבדק חזותית בפועל בכל יחידה — כדאי לוודא שאין חפיפה,
  בפרט במובייל.

### 2026-08-25 — Claude Code — "המאמן שלי לבגרות" עובד בפועל (לא רק "בקרוב")

- **בקשה:** התיקון הקודם (הצגת "בקרוב" למחובר/ת במקום דרישת התחברות)
  תוקן טכנית אבל לא פתר את הבעיה האמיתית — המשתמש עדיין לא מקבל עזרה
  אמיתית בלחיצה על "נתקעת? המאמן כאן".
- **גילוי:** ל-`askBagrutBot` — הבוט שכבר חי ופרוס בבקאנד המשותף (אותו
  `bagrut.gs`), ומשמש בפועל את הווידג'ט הציבורי ב-`tourism11` — **אין שום
  קשר ל-login בכלל** (endpoint פתוח בכוונה, לפי תיעוד `tourism11/CLAUDE.md`).
  כלומר לא הייתה שום סיבה אמיתית שהמאמן ידרוש התחברות מלכתחילה — זו הייתה
  טעות שהכפתור תויג עם `data-login-feature` בכלל.
- **בוצע (`units/coastal-plain.html` + `unit.js` + `styles.css`, בלי צורך
  בפריסת Apps Script — משתמש בפעולה קיימת):**
  - הכפתור "נתקעת? המאמן כאן" הפסיק להיות `data-login-feature`, ופותח
    ישירות ווידג'ט צ'אט חדש (`toggleCoachWidget`).
  - `unit.js`: `callBagrutBot(question,mode,bagrutQuestion)` — אותה חתימה
    בדיוק כמו ב-`tourism11` (`action:'askBagrutBot'`, `mode:'qa'`) —
    `toggleCoachWidget`/`appendCoachMsg`/`sendCoachQuestion` לניהול הצ'אט.
  - עיצוב הווידג'ט (`.coach-toggle`/`.coach-widget`/...) בטוקנים של האתר
    (`--forest`/`--sand`/`--mint`), ממוקם bottom-left מעל `.lesson-footer`
    הקבוע (לא מתנגש איתו — נבדק לפי גובה, לא לפי רוחב מסך).
  - כפתור "שמירת התקדמות" בסיידבר **נשאר** `data-login-feature` כרגיל —
    זה כן דורש חשבון אמיתי, בשונה מהמאמן.
- **נבדק:** `node --check` על `unit.js`/`app.js`. **לא בוצעה בדיקה חזותית
  בדפדפן** — כדאי לוודא שהמאמן פותח, שולח שאלה ומקבל תשובה בפועל.
- **פתוח:** הווידג'ט קיים כרגע **רק במישור החוף** (היחידה שממנה דווח
  הבאג). ירושלים/עמקים/ים המלח/הגליל עדיין בלי ווידג'ט מאמן — אם רוצים
  שם גם, זו הרחבה פשוטה (אותן פונקציות כבר קיימות, רק צריך את המרקאפ
  והכפתור בכל קובץ HTML).

### 2026-08-25 — Claude Code — הרחבת עריכת תוכן לכל 5 היחידות (267 שדות) + תיקון באג "המאמן"

- **בקשה:** המשתמש ביקש שכל התוכן יהיה ניתן לעריכה, לא רק 3 השדות מהפיילוט
  הקודם. הובהר מול המשתמש (AskUserQuestion) שההיקף הוא כל טקסט בכל דף —
  כותרות/כרטיסים/פודקאסטים וכו' — לא רק פסקאות פתיח.
- **בוצע (בלי צורך בשינוי בקאנד — הפעולות הקיימות כבר גנריות):**
  - סקריפט Python חד-פעמי (scratchpad, לא נכנס לריפו) סרק כל אחד מ-5 קובצי
    היחידה, מצא אלמנטים "עלה" (`p`/`h2`/`h3`/`b`/`figcaption`/`li`/`label`
    ללא תגיות מקוננות) בתוך `<main>...</main>`, החריג אזורים בתוך
    `button`/`a`/`nav`/`header`/`aside`/`textarea`/`form`/`script`/`style`
    (כדי לא לגעת בטקסט שקשור למנגנוני משחק/התאמה בקוד JS), והוסיף
    `data-field-key` ייחודי (`unit__page__tagN`) לכל אחד. **267 שדות
    סומנו** (50 במישור החוף, 48 בירושלים, 49/50 בעמקים, 52/53 בים המלח,
    65/66 בגליל — כולל 3 השדות הידניים מהפיילוט שנשארו כפי שהיו).
  - **אימות תקינות** לפני commit: רצף התגיות (`<tag>`/`</tag>`) זהה ביט-לביט
    למקור בכל 5 הקבצים; תוכן הטקסט (אחרי הסרת תגיות) זהה ביט-לביט; כל
    התוכן מחוץ ל-`<main>...</main>` זהה ביט-לביט. כלומר רק תכונת
    `data-field-key` נוספה, שום מבנה/טקסט/סקריפט לא השתנה.
  - `content-overrides.js` (מהרשומה הקודמת) נטען עכשיו גם ב-
    `coastal-plain.html`/`jerusalem.html` (`data-content-unit` נוסף ל-body
    שלהם: `mishor_hachof`/`yerushalayim`).
  - `teacher.js`: טאב "עריכת תוכן" שוכתב — במקום מערך `EDITABLE_FIELDS`
    סטטי, `discoverEditableFields()` שולף `fetch()` את 5 עמודי היחידה,
    מפרש עם `DOMParser`, ואוסף את כל `[data-field-key]` עם הטקסט המקורי
    שלהם כברירת מחדל. מוצג מקובץ לפי יחידה (`<details>` מתקפל) ולפי עמוד,
    עם שדה חיפוש חופשי שמסנן לפי טקסט/מפתח. **מנגנון זה מתעדכן אוטומטית
    אם יתווספו עוד `data-field-key` בעתיד — אין צורך לתחזק רשימה ידנית.**
  - **תיקון נלווה (`app.js`):** התלמיד/ה דיווח/ה בזמן בדיקה שלחיצה על כפתור
    "נתקעת? המאמן כאן" (coastal-plain) מציגה "התחברו דרך כיתה פלוס" **גם
    כשהוא/היא כבר מחובר/ת**. הסיבה: ה-handler הגלובלי ל-`[data-login-feature]`
    לא בדק בכלל אם יש משתמש מחובר לפני שפתח את חלון ההתחברות — וזו הייתה
    ההתנהגות בכל הפיצ'רים המתויגים כך, לא רק במאמן. עכשיו: משתמש/ת מחובר/ת
    רואה "בקרוב" (הפיצ'ר עצמו עוד לא חובר לבקאנד — ידוע ומתועד למעלה),
    ולא הצעת התחברות שגויה.
- **נבדק:** `node --check` על `app.js`/`teacher.js`/`content-overrides.js`.
  **לא בוצעה בדיקה חזותית בדפדפן** (אין גישת דפדפן בסשן הזה) — למרות
  אימות התקינות המבני, שווה לעבור ידנית על כמה עמודים בכל יחידה כדי לוודא
  שהעיצוב לא נפגע ושהעריכה בפועל עובדת קצה-לקצה.
- **פתוח:**
  - טקסט בתוך קוד JS (שאלות/אפשרויות בוחן במישור החוף, `regionOpenQuestions`
    בירושלים/עמקים/ים המלח/הגליל) עדיין **לא** ניתן לעריכה — רק תוכן סטטי
    ב-HTML. הרחבה לשם דורשת גישה שונה (עריכת דאטה בתוך JS, לא data-field-key).
  - כפתור "המאמן" עצמו עדיין לא מציג שום דבר שימושי למשתמש/ת מחובר/ת מעבר
    ל"בקרוב" — חיבורו לבוט קיים (`askBagrutBot`) עדיין בקאנן/לא בוצע.

### 2026-08-25 — Claude Code (הכנת פרומפט) + B (ביצוע בדפדפן) — בקאנד: עריכת תוכן קלה (content_overrides)

- **בקשה:** בקאנד לתמיכה בעריכת טקסטים נבחרים דרך הדשבורד (ר' רשומת
  הפרונטאנד למטה, "עריכת תוכן קלה למורה") — עד עכשיו רק הפרונטאנד היה פרוס
  ו-`getAllContentOverrides`/`saveContentOverride`/`getContentOverrides`
  עדיין לא היו קיימות בקוד.
- **בוצע ב-`bagrut.gs`:** טאב גיליון חדש `content_overrides` (unit_id,
  field_key, text, updated_by, updated_at) דרך
  `ensureBagrutContentOverridesSheet_`. שלוש פונקציות: `getContentOverrides`
  (**פתוחה, בלי login** — גם אורח/ת רואה טקסט ערוך), `getAllContentOverrides`
  ו-`saveContentOverride` (שתיהן מוגנות, דורשות תפקיד teacher/homeroom/
  admin/school_admin; upsert לפי `unit_id`+`field_key`).
- **בוצע ב-`code.gs`:** `getAllContentOverrides`/`saveContentOverride` נוספו
  ל-`protectedActions`; שלושתן נוספו ל-handlers ב-`doPost`.
- **Deploy:** גרסה חדשה לאותו deployment ID הקיים (בוצע ע"י B, לפי פרומפט
  שהכין Claude Code).
- **פתוח:** בדיקה אמיתית — כניסה לטאב "עריכת תוכן" בדשבורד המורה, שמירת
  טקסט לאחד משלושת השדות, ואימות שהוא מופיע בפועל בדף היחידה הציבורי
  (`units/valleys.html`/`dead-sea.html`/`galilee.html`) גם למשתמש/ת שלא
  מחובר/ת (guest).

### 2026-08-25 — Claude Code — פרונטאנד לבדיקת מורה על תשובות פתוחות (משתמש בבקאנד של Version 64)

- **בקשה:** לחבר UI לארבע הפעולות שנפרסו ב-Version 64 (ר' רשומה למטה) — עד
  עכשיו היו רק בקאנד, בלי מסך שמשתמש בהן.
- **בוצע:**
  - `teacher.html`/`teacher.js`: פאנל חדש "תשובות ממתינות לבדיקת מורה" בראש
    טאב "תשובות והערכה" — קורא ל-`getBagrutPendingReviewsForTeacher`, מציג
    כרטיס לכל תשובה עם שדה הערה חופשי ושלושה כפתורי החלטה (אישור/החזרה
    לתיקון/דחייה) שקוראים ל-`reviewOpenAnswer` ומרעננים את התור. כרטיסי
    התשובות של תלמיד/ה בודד/ה (`openStudentAnswers`) מציגים עכשיו תג סטטוס
    (`reviewStatusLabel`/`reviewStatusClass`) והערת מורה אם קיימת.
  - `app.js` (משותף ל-`index.html`, `units/coastal-plain.html`,
    `units/jerusalem.html` — הדפים היחידים שכבר טוענים אותו): באנר הודעה
    לתלמיד/ה מחובר/ת בטעינת הדף, קורא ל-`getMyReviewNotices`; לחיצה על
    "הבנתי" קוראת ל-`ackReviewNotice` ומציגה את ההודעה הבאה בתור אם יש.
  - `styles.css`: מחלקות עיצוב חדשות (`.review-queue-card`, `.review-badge`
    ב-3 גוונים, `.review-notice-banner`/`.review-notice-card`), עוקבות אחרי
    טוקני העיצוב הקיימים (`--forest`/`--sand`/`--mint`/`--line`/`--shadow`).
  - גרסאות cache-busting עודכנו בכל דף ששונה
    (`styles.css?v=17/23/24/16`, `app.js?v=5`, `teacher.js?v=5`).
- **נבדק:** `node --check` על `app.js` ו-`teacher.js` — עבר נקי. **לא בוצעה
  בדיקת דפדפן אמיתית עם login** (אין גישת דפדפן/תצוגה חזותית בסשן הזה).
- **פתוח:**
  - יחידות `valleys.html`/`dead-sea.html`/`galilee.html` לא טוענות את `app.js`
    (מבוססות `region-unit.js` נפרד) — תלמיד/ה שמשתמש/ת רק ביחידות האלה לא
    יראה/תראה את באנר ההודעה עד שהוא ישולב שם גם.
  - אימות ידני קצה-לקצה: התחברות כתלמיד/ה → שליחת תשובה עמומה → התחברות
    כמורה → אישור/החזרה מהתור החדש → חזרה כתלמיד/ה לוודא שההודעה מופיעה
    ונעלמת אחרי "הבנתי".

### 2026-08-25 — Claude Code (הכנת פרומפט) + B (ביצוע בדפדפן) — בקאנד: בדיקת מורה לתשובות פתוחות, הגנת prompt injection, רישום יחידת הגליל

- **בקשה:** לבצע את מסמך "שלב 9" מתיקיית התיאום המשותפת ב-Drive — הפעם היחיד
  שהיה כתוב ומוכן לביצוע מבין 05/06/08/09 שהמתינו ל-B.
- **בוצע ב-`bagrut.gs`** (add-only, לא נגעו בטאב `open_answers` הקיים):
  - שני helpers חדשים: `ensureBagrutOpenAnswerReviewsSheet_` (טאב גיליון חדש
    `open_answer_reviews`) ו-`bagrutSanitizeForPrompt_` (נרמול שורות + הסרת
    רצפי גרשיים/backtick חוזרים לפני הזנה לפרומפט).
  - `submitOpenAnswer` הוחלפה במלואה: מוסיפה בקשת "מידת ביטחון: גבוהה/נמוכה"
    בסוף תשובת הבוט, מפרקת אותה ל-`confidence`, וכותבת שורה מקבילה ל-
    `open_answer_reviews` עם `status: 'auto'` (ביטחון גבוה) או
    `'pending_review'` (ביטחון נמוך). כוללת גם הגנת prompt injection מפורשת
    בפרומפט עצמו (תשובת תלמיד/ה מטופלת תמיד כטקסט-לבדיקה-בלבד, גם אם מנוסחת
    כהוראה) ומזינה עד 3 הערות מורה קודמות דומות באותה יחידה כהקשר.
  - 4 פונקציות חדשות: `reviewOpenAnswer` (מורה מאשר/דוחה/מחזיר לתיקון עם
    הערה), `getBagrutPendingReviewsForTeacher` (תור בדיקה למורה), 
    `getMyReviewNotices`/`ackReviewNotice` (הודעת תלמיד/ה כשמורה בדק/ה תשובה,
    עם אישור קריאה).
  - `getBagrutStudentOpenAnswers` הוחלפה להעשיר כל תשובה בסטטוס/ביטחון/הערת
    מורה מהטאב החדש (לפי מפתח `email|timestamp`).
  - `BAGRUT_UNITS`: נוספה `{ unit_id: 'galil', name: 'הגליל', total_questions: 6 }`
    — סוגר את הפער שתועד ב-24.08 (יחידת הגליל שלחה `unit_id:'galil'` שלא היה
    רשום בבקאנד, אז לא נספרה כהתקדמות/ציון בדשבורד המורה).
- **בוצע ב-`code.gs`** (add-only): 4 השמות החדשים נוספו ל-`protectedActions`;
  4 handlers מתאימים נוספו ל-`doPost` מיד אחרי `submitOpenAnswer`.
- **Deploy:** גרסה חדשה (**Version 64**) לאותו deployment ID הקיים
  (`AKfycbwf3-MNZBBi64zXcNH7wfhBRoEBl9brtQ9QRI4Won5RmUIOrl_WBivN6uI5NAp6Mc0h`)
  — לא נוצר deployment חדש.
- **בוצע ע"י:** B (סשן עם גישת דפדפן אמיתית ל-Apps Script editor, "Claude in
  Chrome"), לפי פרומפט מוכן שהכין Claude Code בסשן `nisan1234-afk.github.io`.
- **פתוח:**
  - בדיקת קצה-לקצה עם login אמיתי (סעיף 9ח במסמך המקורי) — עדיין לא בוצעה
    על ידי אף אחד: תשובה רגילה→`auto`, תשובה עמומה→`pending_review`, קריאה
    ל-`getBagrutPendingReviewsForTeacher`/`reviewOpenAnswer`/
    `getMyReviewNotices`/`ackReviewNotice`, ואימות שיחידת "הגליל" מופיעה
    בטבלת ההתקדמות בדשבורד המורה.
  - הפרונטאנד (`teacher.html`/`teacher.js` וכל דפי היחידות כאן) עדיין לא
    קורא לארבע הפעולות החדשות — אין עדיין UI לתור-בדיקה/אישור-מורה/הודעת-
    תלמיד. הבקאנד מוכן, השכבה הזו טרם נבנתה.
  - `submitOpenAnswer` המעודכנת משותפת ל-`tourism11` ול-`tourism-bagrut-v2`
    (אותו `bagrut.gs`) — כדאי לוודא ש-`tourism11` (הפרונטאנד הישן) לא נשבר
    משינוי הפורמט של תשובת הבוט (הוספת שורת "מידת ביטחון" בסוף).

### 2026-08-23 — Codex — חיבור ראשוני לכיתה פלוס כמקצוע נפרד

- בדשבורד המורה של כיתה פלוס שונה שם המקצוע הקיים ל־"תיירות בגרות — ישן" והקישור ל־tourism11 נשמר.
- נוסף מקצוע "תיירות בגרות — חדש" המקשר למסך המורה של tourism-bagrut-v2 ומסומן "חדש · בהרצה".
- בדשבורד התלמיד הופרד הכרטיס הקיים ל־"ישן" ונוסף כרטיס "חדש" המקשר לאתר החדש.
- זהו בשלב זה חיבור כניסה וניווט בלבד. אימות המשתמש והתקדמות האתר החדש עדיין אינם משתמשים במסד הנתונים של כיתה פלוס.
- מאגר כיתה פלוס: `nisan1234-afk.github.io`; commits: מורה `4802f7f`, תלמיד `59c03c7`.
- פתוח: SSO אמיתי, מזהי תלמיד/כיתה, סנכרון התקדמות, הרשאות מורה ומעבר נתונים מה־localStorage.

### 2026-08-23 — Codex — כיול התכנית לשעה נטו

- המשתמש הבהיר שכל מפגש רביעי/חמישי הוא 90 דקות במערכת אך כ־60 דקות הוראה נטו.
- התכנית כוילה ל־61 שעות נטו עד אמצע מאי ול־5 שעות נטו לכל חבל.
- נוספה תבנית שעה: 5 דקות שליפה, 25 הקניה, 15 פעילות חזותית/פעילה, 10 תרגול ו־5 סיכום.
- העשרה ומשחקים נוספים הוגדרו ללמידה עצמאית כדי להגן על זמן חומר החובה.
- קובץ: `docs/YEAR_PLAN_2026_27.md`; commit: `9de8118`.

### 2026-08-23 — Codex — תכנית שנתית תשפ״ז עד אמצע מאי

- נבנתה תכנית לימודים על בסיס ימי מגמה רביעי–חמישי ולוח החופשות תשפ״ז לחטיבות העליונות.
- נספרו 61 מפגשים זמינים בין 1.9.2026 ל־15.5.2027, לפני ביטולים מקומיים.
- שמונת החבלים קיבלו תבנית של 5 מפגשים כל אחד; שמותיהם יוחלפו כאשר ייקבעו.
- שולבו נושאי־העל מתוך המסמך "שאלות בגרות י״א לפי מיקוד תשפ״א": חשיבות התיירות, גורמי תנועה, סוגי תיירות, מוקדי משיכה, שירותי תיירות ושיווק.
- יעד הקניית חומר חדש נקבע ל־8.4.2027; סוף אפריל עד 13.5 שמורים לתרגול, מתכונת ותיקון פערים.
- נוצר `docs/YEAR_PLAN_2026_27.md` עם חלוקה חודשית, תאריכים זמינים, חופשות, תבנית חבל וכללי ביטחון לתכנית.
- commit: `05eb38d`.
- פתוח: שמות שמונת החבלים, מספר שעות בכל יום, שכבת גיל ומועדי בית־ספר מקומיים.

### 2026-08-23 — Codex — מסך תובנות למורה

- נוסף מסך מורה חדש: `teacher.html`, המקבל את נתוני ההתקדמות וניסיונות התשובות השמורים במכשיר.
- מוצגים ארבעה מדדים: התקדמות ביחידה, סעיפי בגרות שעברו, ממוצע רכיבי תשובה ומספר שיפורים לאחר משוב.
- מוקדי קושי חוזרים מדורגים לפי שכיחות, ולצדם פעולה פדגוגית מומלצת לשיעור הבא.
- טבלת תשע השאלות מציגה בנפרד את מצב סעיפים א׳, ב׳ וג׳ ואת ממוצע הבדיקה לכל שאלה.
- נוסף קישור "תובנות למורה" מתוך יחידת מישור החוף. המסך מסומן כאב־טיפוס מקומי ואינו מתחזה לנתוני כיתה אמיתיים.
- קבצים: `teacher.html`, `teacher.js`, `styles.css`, `units/coastal-plain.html`.
- commits: `0600727`, `aa5e17a`, `4b908e7`, `15c1585`.
- פתוח: החלפת localStorage בנתוני תלמידים מאומתים מכיתה פלוס, הרשאות מורה וסינון לפי כיתה/תלמיד.

### 2026-08-23 — Codex — פיצול שאלות הבגרות לסעיפים עצמאיים

- כל אחת מתשע שאלות הבגרות מפוצלת כעת לסעיפים א׳, ב׳ וג׳, כל סעיף בשורה ובכרטיס נפרד.
- לכל סעיף שדה תשובה, כפתור בדיקה, מונה מילים וריבוע מצב עצמאי.
- סעיף שעבר מקבל סימון ירוק; שינוי התשובה מסיר את הסימון עד לבדיקה חוזרת.
- שאלה מלאה נספרת כהושלמה רק כאשר כל הסעיפים שלה עברו; מד ההתקדמות נשאר ברמת תשע השאלות.
- הטיוטות והניסיונות נשמרים לפי שאלה וסעיף, כהכנה למעקב מדויק של תלמיד ומורה.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- commits: `ce7575e`, `eceaf7a`, `a28ba0c`.

### 2026-08-23 — Codex — היסטוריית ניסיונות ותמונת קושי אישית

- כל בדיקת תשובה פתוחה נשמרת כניסיון: מספר ניסיון, ציון רכיבים, מספר מילים, רכיבים חסרים וזמן.
- משוב חוזר מציג שיפור באחוזים לעומת הניסיון הקודם באותה שאלה.
- מאגר הבגרות מציג תמונת מצב אישית: ממוצע הבדיקות והרכיבים החוזרים שכדאי לחזק.
- תוקן כלל ההשלמה: אורך תשובה לבדו אינו מספיק; שאלה נספרת רק לאחר שעברה בדיקת מחוון.
- עריכת תשובה שכבר עברה מחזירה אותה למצב "מוכן לבדיקה", כדי שהגרסה החדשה תיבדק מחדש.
- הנתונים מקומיים בלבד בשלב זה ומוכנים להעברה עתידית לחשבון התלמיד ולדשבורד המורה.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- commits: `23f5eca`, `d2a5047`, `bd495cf`.

### 2026-08-23 — Codex — שאלות פתוחות עם בדיקה מבוססת מחוון

- שאלת החזרה בעכו הוסבה מתשובה נפתחת לשדה תשובה פתוח שאינו חושף פתרון מראש.
- נוספה שאלה פתוחה לכל אחת מחמש יחידות הלימוד; השלמת היחידה דורשת תשובה שעברה את בדיקת הרכיבים לצד משחקי היחידה.
- כל תשע שאלות הבגרות קיבלו כפתור בדיקה, משוב על רכיבים שנמצאו וחסרים ואפשרות לשפר ללא הגבלת ניסיונות.
- הבדיקה הנוכחית מקומית ודטרמיניסטית, מבוססת רק על מחוונים ומונחים מחומרי היחידה המאושרים. היא מכינה את הממשק לחיבור עתידי לבוט בצד שרת ואינה מוצגת כ-AI חיצוני.
- הטיוטות ותוצאות המעבר נשמרות במכשיר. תשובות דוגמה אינן מוצגות מראש.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- commits: `14f6e27`, `879f6a8`, `170c6b3`.
- פתוח: חיבור בודק התשובות לבוט מאובטח בצד שרת, שמירת ניסיונות למורה וכיול מחוונים על תשובות תלמידים אמיתיות.

### 2026-08-23 — Codex — מעבר מגלילה ארוכה לדפי לימוד

- יחידת מישור החוף הוסבה ממסמך אחד ארוך למסלול בן 10 דפים, כאשר בכל רגע מוצג רק דף לימוד אחד.
- חמש יחידות התוכן, סיכום התרגולים, זיהוי התמונות, המצגת, הבוחן ומאגר הבגרות קיבלו כתובת פנימית נפרדת.
- נוספו כפתורי דף קודם/הבא, מונה דפים וכותרת הדף הנוכחי; התפריט הצדדי מסמן את הדף הפעיל.
- הדף האחרון נשמר מקומית, וקישור ישיר עם # מחזיר לאותו דף.
- בנייד סרגל המעבר נשאר נגיש בתחתית המסך.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- commits: `5982119`, `67c2522`, `05a5877`.
- פתוח: בדיקת שימוש אמיתית בנייד והחלטה אם לפצל גם יחידות ארוכות לתת־דפים.

### 2026-08-23 — Codex — חלוקת מישור החוף לחמש יחידות לימוד

- תוכן מישור החוף אורגן מחדש לחמש יחידות: קוראים את המרחב; טבע, מורשת ודת בצפון; קיסריה והכרמל; תל אביב–יפו; אשקלון ושפלת יהודה.
- לכל יחידה נוספו מטרות למידה, הבחנת חובה/העשרה, מיומנות בגרות, תרגול מקומי, תנאי השלמה וכפתור המשך אחד.
- עשרת המשחקים פוזרו בתוך היחידות לפי מטרתם: 2/3/2/2/1 פעילויות בהתאמה.
- אזור המשחקים המרכזי הפך למסך מעקב וסיכום עם קישורים חזרה לחמש היחידות.
- יחידה אינה מסומנת כהושלמה אם הפעילויות המשויכות אליה לא הושלמו; ניתן לדלג אך הדילוג אינו נספר.
- תוקן עוגן המבוא כך שהגאוגרפיה והנוף הם יחידת לימוד מלאה ולא חלק מהפתיח החזותי.
- בדיקות חיות: 5 יחידות, 5 אזורי תרגול, 10 משחקים, חלוקה 2/3/2/2/1, 5 כרטיסי מעקב, עברית תקינה.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`, `docs/UNIT_EXPANSION_PLAYBOOK.md`.
- commits: `e2a6656`, `0f436ac`, `44bb86c`, `ab604ab`, `a28405e`.
- פתוח: העברת הנתונים מ־`unit.js` למודל יחידה נפרד והוספת מחוון רכיבים לתשובות פתוחות.

### 2026-08-23 — Codex + מועצת AI — ביקורת עומק לפני שכפול

- נערכה מועצה בת חמש זוויות: מתנגד, עקרונות יסוד, הרחבה, משתמש חיצוני וביצוע, ולאחריה ביקורת עמיתים ויו״ר מסכם.
- החלטה: לא לשכפל עדיין את מישור החוף כפי שהוא. קודם להפוך אותו ליחידת־אב המבוססת על נתונים ומנוע משותף.
- הוגדרו ארבע שכבות: תוכן מובנה, חוזה פדגוגי, תבנית מוצר ואירועי למידה.
- זוהו פערים מרכזיים: הגדרת שליטה מדידה, בקרת שלמות מול Drive, גרסאות ואישור פרסום, שמירת התקדמות לאחר שינוי, מחוון לתשובות פתוחות, נגישות ולולאת איכות לאחר פרסום.
- `docs/UNIT_EXPANSION_PLAYBOOK.md` עודכן בפרק 9 עם שער לפני שכפול, סדר עדיפויות P0/P1/P2 ומבחן מעבר ליחידה שנייה.
- היכולות הראשונות המומלצות לאחר ייצוב: מילון בהקשר, "מה שכחתי", מצב חזרה לבגרות ודשבורד מורה שממליץ פעולה.
- commit: `f39e137`.
- הפעולה הבאה: להוציא את נתוני מישור החוף מתוך `unit.js` למודל יחידה מובנה, בלי לשנות את חוויית המשתמש החיה.

### 2026-08-23 — Codex — מדריך שכפול יחידות וביקורת הפרויקט המקורי

- נוצר מסמך מחייב חדש: `docs/UNIT_EXPANSION_PLAYBOOK.md`.
- המסמך מפרט כיצד לשכפל את יחידת מישור החוף לכל חבל ארץ ולנושאים שאינם גאוגרפיים: מקורות, מבנה נתונים, מפת תוכן, עמוד תלמיד, משחקים, מצגת, הערכה, התקדמות, נגישות, בדיקות והגדרת "יחידה מוכנה".
- נסקרו בפרויקט `tourism11`: `CONTENT_GUIDE.md`, `CLAUDE.md`, דשבורד תלמיד, דשבורד מורה, מישור החוף, ים המלח, העמקים והמושגים.
- יכולות מומלצות להעברה: "מה שכחתי", מילון צף, משוב על עמוד, בדיקת תשובה פתוחה בשלבים, היסטוריית טעויות, תכנון מסלול, מאגר זיהוי התמונות, דשבורד מורה, ייבוא תלמידים ובוט סוקרטי.
- המסמך מפריד בין יכולות שכדאי להעביר, יכולות שיש לבנות מחדש ורכיבים שאין להעביר כפי שהם.
- commit: `071a4df`.
- המסמך הוא נקודת הפתיחה לכל יחידה חדשה ויש לעדכנו כאשר מתגלה כלל מוצר חדש.

### 2026-08-23 — Codex — החלפת המפה גם במשחק 7

- בעקבות הערת המשתמש, גם משחק 7 "על המפה" הועבר מהמפה הסכמטית למפת ישראל הגאוגרפית האמיתית.
- שבע נקודות המשחק משתמשות כעת באותן קואורדינטות של המפה האילמת, ולכן שני משחקי המפה עקביים.
- נוסף קרדיט למקור המפה גם במשחק 7.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- בדיקה חיה: `unit.js?v=14`, `styles.css?v=4`, כותרת "על מפת ישראל", רקע Wikimedia, יחס 461/856 וכל שבע הנקודות במיקומים החדשים.
- commits: `2ede841`, `46a6457`, `c718efb`.

### 2026-08-23 — Codex — מפה גאוגרפית אמיתית ושילוב מאגר התמונות

- המפה הסכמטית במשחק המפה האילמת הוחלפה במפת ישראל אמיתית ביחס 461×856, עם קו חוף וגבולות גאוגרפיים.
- שבע נקודות השיבוץ מוקמו לפי קווי רוחב ואורך בקירוב: ראש הנקרה, עכו, חיפה, קיסריה, תל אביב–יפו, אשקלון ובית גוברין.
- מקור המפה: Wikimedia Commons, הקובץ `Israel location map current borders.svg`, ברישיון CC BY-SA 3.0; נוסף קרדיט וקישור בתוך המשחק.
- נסרק מאגר `jerusalem-tour` ובו 49 תמונות ייעודיות למישור החוף.
- נוספו 12 תמונות מקוריות כפסי סיפור חזותיים המשולבים ישירות בארבעת פרקי הלימוד: צפון, כרמל, תל אביב–יפו ודרום.
- התצוגה מותאמת לנייד כקרוסלת פוסטרים אופקית.
- קבצים: `unit.js`, `styles.css`, `index.html`, `units/coastal-plain.html`.
- בדיקות חיות: `unit.js?v=13`, `styles.css?v=3`, 12/12 תמונות נטענות, רקע מפת Wikimedia ויחס מפה תקין.
- commits מרכזיים: `9030623`, `61a3bb6`, `ba3a868`, `4ea0de6`, `8c7010d`.

### 2026-08-23 — Codex — תיקון קידוד עברית באתר החי

- תוקנה תקלה שבה קבצי האתר הועלו מחדש בקריאת קידוד שגויה וגרמו לטקסט דינמי להופיע כג׳יבריש, במיוחד בטלפון.
- כל חמשת קבצי האתר הועלו מחדש כ־UTF-8 תקין: `index.html`, `app.js`, `styles.css`, `unit.js`, `units/coastal-plain.html`.
- נוספו מזהי גרסה חדשים לנכסים כדי לנקות עותקים ישנים ממטמון הדפדפן: `styles.css?v=2`, `app.js?v=2`, `unit.js?v=12`.
- בדיקה חיה: כותרת היחידה, "עשרה אתגרים קצרים", "מפה אילמת" ו־"10 מתוך 10 משחקים הושלמו" מוצגים בעברית תקינה.
- commits מרכזיים: `71c2100`, `f7a81d0`, `515460c`, `2be0ac6`.

### 2026-08-23 — Codex — משחק מפה אילמת

- נוסף משחק עשירי: מפה אילמת של מישור החוף עם 7 מקומות לשיבוץ — ראש הנקרה, עכו, חיפה, קיסריה, תל אביב–יפו, אשקלון ובית גוברין.
- במחשב ניתן לגרור שמות אל נקודות; בטלפון בוחרים שם ולוחצים על הנקודה המתאימה.
- שמות המקומות מעורבבים, נקודות המפה נשארות ריקות, וטעות מציגה הכוונה לבדוק את הסדר מצפון לדרום.
- ניתן לאפס את המפה; השלמה מלאה של 7/7 מעדכנת את משחקי היחידה ל־10/10.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- בדיקות חיות: 7 תוויות, 7 יעדים, משוב לטעות, איפוס, השלמה 7/7 וסטטוס 10/10.
- commits: `4d0a6b0`, `58a7fec`, `a4bf079`.
- פתוח: החלפת המפה הסכמטית במפת ישראל מדויקת יותר והרחבת השיבוץ לאתרי משנה.

### 2026-08-23 — Codex — משוב לימודי ושיפור הוגנות המשחקים

- מפת החוף אינה חושפת עוד את שמות האתרים מראש; מוצגות 7 נקודות ממוספרות והתלמיד נדרש לזהות את המיקום.
- לאחר טעות במפה או במשחק נכון/לא נכון מוצג הסבר קצר מתוך חומרי היחידה לפני המעבר לשאלה הבאה.
- אפשרויות התשובה באתגר הזמן מתערבבות בכל שאלה, כך שהתשובה הנכונה אינה נשארת במקום קבוע.
- נוספה נעילת לחיצות קצרה כדי למנוע דילוג כפול בזמן הצגת המשוב.
- קבצים: `unit.js`, `units/coastal-plain.html`.
- בדיקות חיות: גרסה `v10`, 7 נקודות ממוספרות, משוב טעות מפורט, מעבר תקין לסיבוב הבא ואפשרויות מעורבבות.
- commits: `494ddda`, `946f2d9`.
- פתוח: משוב מסביר גם למשחקי ההתאמה הראשונים והיסטוריית טעויות לתלמיד המחובר.

### 2026-08-23 — Codex — תשעה משחקי למידה במישור החוף

- נוספו שלושה משחקים נוספים: איתור 5 אתרים על מפת החוף, רצף של 5 תשובות נכון/לא נכון ואתגר מהיר של 6 שאלות ב־45 שניות.
- יחידת המשחקים כוללת כעת 9 משחקים; השלמות מששת המשחקים הקודמים נשמרות, והשלמת השלב דורשת הצלחה בכל התשעה.
- משחק המפה כולל 7 נקודות ציון ומגריל 5 בכל סיבוב; מעבר דורש 4 תשובות נכונות. האתגר המהיר דורש 5 מתוך 6.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- בדיקות חיות: 9 כרטיסי משחק, הצלחה 5/5 במפה, רצף 5/5, הצלחה 6/6 בזמן, סטטוס 9/9 וסימון שלב המשחקים כהושלם.
- commits: `1ba41bc`, `ffe5dde`, `4953c77`.
- פתוח: הוספת משוב מסביר לאחר טעויות, ערבוב אפשרויות מתקדם וניקוד היסטורי לתלמיד המחובר.

### 2026-08-23 — Codex — הרחבת משחקי הלמידה לשישה

- נוספו שלושה משחקים לבקשת המשתמש: משחק זיכרון עם 6 זוגות של אתר ועובדה, התאמת 5 מושגים להסברים, ופאזל תמונה 3×3 של ראש הנקרה.
- יחידת המשחקים כוללת כעת 6 משחקים; ההשלמות הקודמות נשמרות והשלב כולו מושלם רק לאחר הצלחה בכולם.
- פאזל התמונה מאפשר ערבוב והחלפת שני חלקים בלחיצה, וכל המשחקים ניתנים לביצוע חוזר.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- בדיקות חיות: 12 קלפי זיכרון, 5 התאמות מושגים, 9 חלקי פאזל, הצלחה מלאה 6/6 וסימון שלב המשחקים כהושלם, ללא שגיאות מסוף.
- commits: `d6e6ab5`, `57cf031`, `059bf5d`.
- פתוח: משחק מפה, אתגר זמן, רצף נכון/לא נכון וניקוד היסטורי לתלמיד המחובר.

### 2026-08-23 — Codex — משחקי למידה במישור החוף

- נוסף שלב עשירי ובו שלושה משחקי למידה קצרים: התאמת אתר לאזור, "מי אני?" מחמישה רמזים, וסידור מסלול מצפון לדרום.
- לכל משחק מצב הצלחה עצמאי; המשחקים ניתנים לביצוע חוזר והשלב נרשם כהושלם רק לאחר הצלחה בשלושתם.
- מצב השלמת המשחקים נשמר מקומית ומתחבר למד ההתקדמות של היחידה.
- העיצוב מותאם גם למסכים קטנים ונשאר מקצועי ולא ילדותי.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- בדיקות חיות: 10 שלבים, 3 משחקים, השלמה 3/3, סימון שלב המשחקים כהושלם וללא שגיאות מסוף.
- commits: `d8851b8`, `2a5444f`, `1fc2fc5`.
- פתוח: הוספת משחק זיכרון חזותי, ערבוב שאלות דינמי וניקוד היסטורי לתלמיד המחובר.

### 2026-08-23 — Codex — מאגר מלא של שאלות בגרות מישור החוף

- מאגר השאלות הפתוחות הורחב מ-3 ל-9 שאלות מתוך `שאלות בגרות מישור החוף.docx` בתיקיית חומרי הגלם.
- נשמרו כל שלושת הסעיפים של כל שאלה, ונוספו תיוגי נושא, מחוון ותשובת דוגמה המבוססת על חומרי המקור המאושרים.
- אין בקובץ שנת בחינה או מספר שאלון; באתר הן מסומנות כ"שאלות בגרות מחומרי המקור" ללא שנה מומצאת.
- נוסף מד התקדמות עצמאי 0/9, מצב השלמה לכל תשובה ושמירת טיוטות נפרדת.
- השלמת שלב הבגרות נרשמת רק לאחר מענה של לפחות 25 מילים בכל תשע השאלות.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- בדיקות חיות: 9 שאלות מוצגות, מד התקדמות מתעדכן מ-3/9 ל-4/9, הטיוטה נשמרת לאחר רענון, ללא שגיאות מסוף.
- commits: `ac2ee93`, `808f942`, `fa7fa0a`.
- פתוח: פיצול כל שאלה לשלושה שדות סעיפים לצורך מעקב מדויק יותר, חיבור תשובות למורה וכיתה פלוס.

### 2026-08-23 — Codex — העמקת קריאה ותרגול בגרות פתוח

- נקראו חוברת התרגול, חוברת המחוון וקובץ שאלות הבגרות המאושרים ב-Drive.
- נוספו 8 כרטיסי העמקה מסוג "להבין את התמונה" ו"לבגרות" בארבעת אזורי התוכן המרכזיים.
- נוספה יחידת משנה תשיעית ובה 3 שאלות פתוחות: נוף ותיירות בדרום, יפו והעיר הלבנה, ותכנון סיור היסטורי.
- הטיוטות נשמרות מקומית, מוצג מונה מילים, ויש מחוון ותשובת דוגמה לכל שאלה.
- תשובות ריקות אינן מסומנות כהשלמה; השלמה נרשמת רק אחרי לפחות 25 מילים בכל תשובה.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- בדיקות חיות: 9 שלבים, 8 כרטיסי העמקה, 3 שאלות פתוחות, שמירת טיוטה, חסימת תשובות קצרות, השלמה תקינה וללא שגיאות מסוף.
- commits: `dc1b112`, `5fa769d`, `81a2e35`.
- פתוח: השלמת כל פרטי החוברת שלא שולבו, שילוב תמונות במצגת, היסטוריית ניסיונות בצד שרת וחיבור לכיתה פלוס.

### 2026-08-23 — Codex — תמונות ואתגר זיהוי חזותי

- שולבו 17 תמונות אמיתיות מספריית חומרי הגלם הקיימת בכרטיסי יחידת מישור החוף.
- נוסף שלב שמיני ליחידה: זיהוי 8 אתרים מתמונה עם שלוש אפשרויות, משוב מיידי וסיכום תוצאה.
- שלב התמונות נכלל בחישוב ההתקדמות של היחידה.
- תוקנה התנגשות בין מונה אתגר התמונות למונה הבוחן.
- קבצים: `unit.js`, `styles.css`, `units/coastal-plain.html`.
- בדיקות חיות: 8 שלבים, 17 תמונות, מונה תמונות 1/8, מונה בוחן 1/10, מעבר לתמונה 2/8 וללא שגיאות מסוף.
- commits: `9e868d0`, `840dbb9`, `443b671`, `6c50ec5`, `f51ecbc`.
- פתוח: העמקת קטעי קריאה, שאלות פתוחות בסגנון בגרות, תמונות במצגת עצמה וחיבור לכיתה פלוס.

### 2026-08-23 — Codex — הרחבת תוכן מישור החוף

- הורחבה יחידת הפיילוט מחומרי המקור המאושרים ב-Drive.
- המצגת המוטמעת הורחבה מ-6 ל-28 שקופיות ושומרת על סדר הלימוד מצפון לדרום.
- נוספו 22 כרטיסי חומר: אתרים נוספים בעכו ובגליל המערבי, קיסריה והכרמל, תל אביב והדרום.
- הבוחן הורחב מ-3 ל-10 שאלות; ציון מעבר 60, הצגת התשובה הנכונה ואפשרות לניסיון נוסף.
- קבצים: `unit.js`, `styles.css`.
- בדיקות חיות ב-GitHub Pages: 22 כרטיסים, מונה 1/28, שאלה 1/10, דפדוף ל-2/28, משוב תשובה וללא שגיאות מסוף.
- commits: `7fbd986`, `5f6335a`.
- פתוח: שילוב תמונות אמיתיות, העמקת קטעי הקריאה מתוך החוברות, תרגול פתוח בסגנון בגרות וחיבור לכיתה פלוס.

### 2026-08-23 — Codex — יחידת פיילוט מישור החוף

- נבנתה יחידת לימוד חיה מתוך חומרי Drive המאושרים.
- נוספו: ניווט פנימי, שבעה שלבי התקדמות, תוכן לפי הסדר הפדגוגי, מצגת אינטראקטיבית באתר ובוחן מסכם בציון מעבר 60.
- ההתקדמות נשמרת כרגע מקומית בדפדפן לצורכי הדגמה; טרם מחוברת לכיתה פלוס.
- קבצים: `index.html`, `styles.css`, `app.js`, `unit.js`, `units/coastal-plain.html`.
- בדיקות: `node --check`, טעינה חיה ב-GitHub Pages, סימון חלק והתקדמות ל-14%, דפדוף במצגת.
- תוקן שימוש בסקריפט המשותף בעמוד יחידה ללא רכיבי דף הבית.
- פתוח: שילוב תמונות אמיתיות, הרחבת המצגת לכל החומר, חיבור התקדמות וכניסה לכיתה פלוס.

### 2026-08-23 — Codex

- נוצר המאגר הציבורי `nisan1234-afk/tourism-bagrut-v2`.
- המטרה: לבנות גרסה חדשה לצד האתר הפעיל ולא להמשיך בתיקוני טלאים.
- נוסף תיעוד חזון, ארכיטקטורה ותיאום.
- האתר הפעיל וחיבור כיתה פלוס לא שונו.


### 2026-08-23 — Codex — מיצוב כחוויית "מאמן אישי לבגרות"

- **בקשה:** למצב ולעצב את האתר עבור בני 17 כמוצר שלם ומקצועי.
- **בוצע:** מסך הפתיחה של יחידת מישור החוף הוסב לחוויית מאמן אישי: משימה הבאה, זמן משוער, אחוז מוכנות חי, מדדי הצלחה ושפה קצרה ובוגרת. עודכנו שמות שלבים וכפתור הבוט ל"המאמן שלי לבגרות". כל התוכן והפעילויות הקיימים נשמרו.
- **קבצים:** `units/coastal-plain.html`, `unit.js`, `styles.css`, `app.js`.
- **בדיקות:** `node --check` עבר עבור `unit.js` ו-`app.js`; נוספה התאמת מובייל; אירועי התחברות שונו להאזנה דינמית כדי שגם כפתור המאמן החדש יעבוד.
- **Push:** בוצע ל-`main` בארבעה commits דרך GitHub.
- **הבהרה:** כפתור המאמן עדיין פותח שער התחברות; חיבורו לבקאנד הבוט הקיים הוא השלב הבא. נתוני ההתקדמות עדיין מקומיים בדפדפן עד לחיבור למסד כיתה פלוס.


### 2026-08-24 — Codex — שילוב תמונות מחומרי הגלם וממצגות NotebookLM

- **בקשה:** להשתמש בתמונות ובפוסטרים שבתיקיית חומרי הגלם של אתר תיירות אלוני הבשן.
- **מקור:** `חבלים/מישור החוף/חומר גלם` ומצגת `מישור_החוף_מצגת (1).pptx` ב-Drive. המשתמש אישר במפורש לפרסם את התמונות באתר הציבורי.
- **בוצע:** חולצו 12 תמונות, הומרו ל-WebP מותאם לרשת (סה״כ כ-1.62MB), הועלו ל-`assets/coastal/` ושולבו ברצועות החזותיות ובכרטיסי התוכן של צפון החוף, קיסריה והכרמל, תל אביב–יפו והחוף הדרומי. נוסף ציון מקור גלוי.
- **קבצים:** 12 קובצי WebP חדשים, `unit.js`, `styles.css`, `units/coastal-plain.html`.
- **בדיקות:** `node --check unit.js` עבר; גרסאות cache עודכנו ל-`unit.js?v=23` ו-`styles.css?v=12`; התמונות נטענות ב-lazy loading.
- **Push:** בוצע ל-`main`.
- **פתוח:** לבדוק חזותית באתר החי לאחר השלמת פריסת GitHub Pages ולהמשיך את אותו דפוס לחבלים הבאים.


### 2026-08-24 — Codex — תיקון כניסה לכיתה פלוס והגנת דף המורה

- **דיווח משתמש:** קישור המורה היה גלוי לכולם וכפתור ההתחברות לא הפנה לכיתה פלוס.
- **בוצע:** כל כפתור ההתחברות בחלון הכניסה מפנה כעת ל-`https://nisan1234-afk.github.io/`; קישור המורה ביחידת התלמיד מוסתר ומוצג רק לפי `sessionStorage.kitahUser` ותפקיד staff; `teacher.html` בודק תפקיד מורה/מנהל לפני הצגת התוכן ומציג מסך כניסה מוגן במקרה שהפניה אוטומטית נחסמת.
- **תפקידי staff מאושרים:** `admin`, `school_admin`, `teacher`, `homeroom`.
- **קבצים:** `app.js`, `index.html`, `units/coastal-plain.html`, `teacher.html`, `styles.css`.
- **בדיקות:** `node --check app.js` עבר; בדיקה חיה אישרה שתוכן המורה מוסתר כאשר אין הרשאה. עודכנה גרסת cache ל-`app.js?v=4` ול-`styles.css?v=13` בדף המורה.
- **הבהרה:** זו הגנת ממשק מתאימה לנתוני ההדגמה המקומיים. עם חיבור נתוני תלמידים אמיתיים חובה לשמור גם על בדיקת הרשאות בצד Apps Script בכל קריאת API.

## 2026-08-24 — דשבורד ניהול מורה מחובר (שלב ראשון)

- הוחלף מסך התובנות המצומצם במרכז ניהול בעל חמישה אזורים: סקירה, תלמידים וכיתות, יחידות ותוכן, תשובות והערכה ותובנות.
- חוברו הפעולות הקיימות של מערכת תיירות/כיתה פלוס: טעינת רשימת תלמידים (`getBagrutTeacherDashboard`), הוספת תלמיד (`addBagrutStudent`) וטעינת תשובות פתוחות לתלמיד (`getBagrutStudentOpenAnswers`).
- נשמרה הגנת ההרשאות: רק תפקידי מנהל/מורה/מחנך שנכנסו דרך כיתה פלוס יכולים לפתוח את המסך.
- נוספה הודעה ברורה כאשר חסר חיבור תקף במקום להשאיר את המסך במצב "טוען".
- אזור יחידות ותוכן מציג כרגע את יחידת מישור החוף וקישורים ליחידה ולמצגת. עורך התוכן המלא עדיין אינו מחובר.
- התובנות הקיימות עדיין מבוססות על נתוני המכשיר המקומי; בשלב הבא יש להעביר גם ניסיונות, ציונים והתקדמות למסד הנתונים המרכזי.
- טרם הושלמו: ניהול כיתות וקבוצות מלא, ייבוא תלמידים מרוכז, מחיקה/עריכת תלמיד, פתיחה וסגירה של יחידות ועורך תוכן מלא.

### המשך דשבורד המורה — ניהול תלמידים ותמונת מצב

- נוסף ייבוא מרוכז בהדבקת שורות בפורמט שם, דוא״ל, כיתה, דרך הפעולה הקיימת `addBagrutStudentsBulk`.
- נוספה אפשרות הסרה עם אישור מפורש דרך `removeBagrutStudent`.
- נוספה תמונת מצב המחושבת מנתוני הכיתה: טרם התחילו, זקוקים לחיזוק, בדרך הנכונה והתקדמות ממוצעת.
- נוסף סינון תווים בטקסטים המגיעים ממסד הנתונים לפני הצגתם בדף.
- נבדק תקינות תחביר קוד הדשבורד לפני ההעלאה.

## 2026-08-24 — חיבור נתוני תלמיד לכיתה פלוס

- אותרו ואומתו פעולות ה־API המקוריות מתוך `tourism11`: `getBagrutMyProgress`, `submitOpenAnswer`, `saveBagrutQuizResult`, `updateBagrutMistakes` ופעולות דשבורד המורה.
- יחידת מישור החוף החדשה משתמשת כעת באותו מזהה יחידה קיים: `mishor_hachof`.
- בסיום התרגול המסכם נשמרים למשתמש מחובר הציון ומספר השאלות דרך `saveBagrutQuizResult`.
- כל שאלה פתוחה, הן בתוך פרקי הלימוד והן במאגר שאלות הבגרות, נשלחת למשתמש מחובר דרך `submitOpenAnswer`; משוב הבוט מוצג לצד הבדיקה המקומית.
- בגרסה הפתוחה אין שליחת נתונים לשרת, וההתנסות נשמרת מקומית בלבד.
- שמירת התקדמות ברמת כל דף עדיין מקומית: ה־API הקיים שומר כרגע תוצאות יחידה ושאלות אך אינו חושף פעולה נפרדת להשלמת דף. אין להציג זאת עדיין כהתקדמות מרכזית מלאה.
- בוצעה בדיקת תחביר ל־`unit.js` לפני העלאה.

## 2026-08-24 — תחילת יחידת ירושלים

- לאחר השלמת החיבורים הזמינים לכיתה פלוס, נבדק שאין מאגר GitHub נגיש המכיל את קוד Apps Script; לכן שמירת דפים פרטנית לא נוספה לשרת ללא מקור מוסמך.
- פורסמה גרסה ראשונה של יחידת ירושלים ב־`units/jerusalem.html` ונוסף קישור פעיל מדף הבית.
- היחידה מחולקת לשישה דפים: מבנה העיר, ארבעת הרבעים, הר הבית, ירושלים הנוצרית והר הזיתים, העיר החדשה ותרגול מסכם.
- נוסף מנוע משותף `region-unit.js` לשכפול מבוקר לחבלים הבאים.
- התרגול שומר ציון וטעויות בכיתה פלוס למשתמש מחובר, והשאלה הפתוחה נשלחת לבוט ונשמרת למורה.
- זוהי גרסת תוכן ראשונה בלבד. עדיין נדרשים: שילוב מאגר התמונות והמצגות מירושלים, הרחבת שאלות הבגרות, משחקים, מפה אילמת ובדיקת מובייל מלאה.

### הרחבת ירושלים — תמונות, מפה, מצגת ובגרות

- שולבו שש תמונות אמיתיות מתוך `jerusalem-tour` במשחק זיהוי אתרים.
- נוספה מפה אילמת אינטראקטיבית על גבי מפת העיר העתיקה האמיתית מ־Wikimedia Commons; התלמיד משבץ את ארבעת הרבעים.
- נוספה מצגת פנימית בת שש שקופיות, באותו סדר של דפי הלימוד, כולל מצב מסך מלא. היא אינה תלויה בהרשאות קובץ Drive.
- נוספה שאלת סיום פתוחה לכל אחד מחמשת דפי התוכן. דף אינו מסומן כהושלם לפני תשובה שעברה את בדיקת הרכיבים; למשתמש מחובר התשובה נשמרת ונשלחת לבוט.
- אותר ונקרא מסמך `שאלות בגרות ירושלים (9).docx` בדרייב.
- נוספו שני מקבצי שאלות בגרות אמיתיות, שישה סעיפים בסך הכול. כל סעיף מוצג בתיבה נפרדת, נבדק בנפרד וניתן לניסיון חוזר ללא הגבלה.
- ירושלים נוספה כיחידה פעילה גם לדשבורד המורה, עם קישור ישיר ליחידה ולמצגת.
- אותרו חומרי המקור המלאים לעמקים והוכן מיפוי ראשוני: גיאוגרפיה, ארכאולוגיה, מים וטבע, התיישבות חקלאית ושאלות בגרות אמיתיות.


## 2026-08-24 — יחידת העמקים
- נבנתה יחידת לימוד חדשה ומלאה ב־`units/valleys.html`, מחולקת ל־7 דפים כדי למנוע גלילה אינסופית.
- נושאי היחידה: מבנה החבל, ארכיאולוגיה, מים וטבע, התיישבות, זיהוי חזותי, מצגת ותרגול בגרות.
- שולבו תמונות אמיתיות ממאגר חומרי הגלם הקיים: הגלבוע, בית שאן, גן השלושה, בית שערים ותל מגידו.
- נוספו שאלות פתוחות ובנק שאלות בגרות, כאשר כל סעיף מופיע בשורה ובתיבת תשובה נפרדת ונשלח לבדיקה דרך מנגנון כיתה פלוס.
- נוספו שמירת התקדמות מקומית, זיהוי תמונות ומצגת פנימית במסך מלא.
- היחידה חוברה לדף הבית ולדשבורד המורה.
- כתובת ישירה: https://nisan1234-afk.github.io/tourism-bagrut-v2/units/valleys.html#overview

- תיקון מובייל לעמקים: נוספה מעטפת רספונסיבית מלאה ליחידות העצמאיות. בוטלה גלישה אופקית של תמונות 1600px, התפריט הפך למגירה, הכרטיסים והתרגילים נערמים לטור אחד, והמצגת/ניווט הותאמו למסך צר. נבדק בפועל ברוחב 390px: רוחב המסמך 375px וללא גלישה הצידה.


## 2026-08-24 — יחידת ים המלח ומדבר יהודה
- נבנתה יחידה חדשה ב־`units/dead-sea.html`, המחולקת ל־7 דפים: גאוגרפיה וצל גשם, צפון האזור, עין גדי ומצדה, דרום/מלח ומרפא, זיהוי תמונות, מצגת ותרגול פתוח.
- התוכן נשען על `yam-hamelach.html` מהפרויקט הישן. שולבו תשע תמונות המקור ממאגר jerusalem-tour.
- מאחר שבמקור הישן מצוין שאין שאלות בגרות מאומתות לאזור, התרגילים מסומנים כשאלות חזרה פתוחות ולא כשאלות בגרות אמיתיות.
- נוספו בדיקת תשובות דרך כיתה פלוס, שמירת התקדמות, זיהוי תמונות ומצגת פנימית.
- היחידה חוברה לדף הבית ולדשבורד המורה.
- נבדקה בתצוגת 390px: אין גלישה אופקית, סרגל הניווט בגובה 44px ומעבר הדפים עובד ללא שגיאות.
- כתובת: https://nisan1234-afk.github.io/tourism-bagrut-v2/units/dead-sea.html#overview


## 2026-08-24 — יחידת הגליל
- נבנתה יחידה חדשה בת 8 דפים: מבוא, גליל עליון ועמק החולה, גליל תחתון וסובב כנרת, גליל מערבי ועכו, דת/קהילה/התיישבות, זיהוי, מצגת ותרגול.
- מקורות התוכן: שלוש מצגות הדרייב גליל עליון, גליל תחתון וגליל מערבי/מישור חוף צפוני. מפת המקורות נשמרה ב־docs/GALILEE_SOURCE_MAP.md.
- שולבו שאלות פתוחות הנשמרות לכיתה פלוס. תרגילים לא סומנו כשאלות בגרות אמיתיות עד להשלמת האימות מול מסמך המקור.
- שולבו תמונות תיעודיות של הארבל/כנרת, ראש הנקרה ועכו; התוכן עצמו נשאר מבוסס על חומרי המגמה בלבד.
- היחידה חוברה לדף הבית ולדשבורד המורה.
- נבדקה ברוחב 390px: רוחב מסמך 375px, סרגל ניווט 44px, שמונה דפים, מעבר תקין, אין תמונות שבורות ואין שגיאות מסוף.
- כתובת: https://nisan1234-afk.github.io/tourism-bagrut-v2/units/galilee.html#overview

- תיקון מדיניות תמונות בגליל: כל התמונות החיצוניות שהוכנסו בגרסה הראשונה הוסרו מיד מהדפים, ממשחק הזיהוי ומהמצגת. נבדק אתר התמונות jerusalem-tour: יש בו 114 תמונות אך אין בו סדרת גליל. עד שיועלו תמונות גליל לאתר התמונות, היחידה תוצג ללא תמונות ולא תשתמש במקור חיצוני או בתמונה של חבל אחר. בדיקת האתר החי אישרה 0 כתובות תמונה חיצוניות.

- השלמת גליל לפני מעבר למשימת הסדר: שולבו 6 שאלות בגרות אמיתיות שאומתו במסמך הדרייב, כל אחת בסעיף ובתיבת תשובה נפרדים. בדיקה חיה ברוחב 390px: 6 שאלות, 6 תיבות, סרגל 44px, רוחב מסמך 375px, 0 תמונות ו־0 מקורות תמונה חיצוניים. דף התמונות נשאר במפורש בהמתנה להוספת תמונות גליל לאתר התמונות הרשמי.


## 2026-08-24 — תקן מחייב לכל יחידה
- נוצר המסמך `docs/UNIT_STANDARD_SPEC_HE.md` כ־source of truth לכל יחידה קיימת או עתידית.
- המסמך מרכז את הדרישות העיצוביות, הפדגוגיות והטכניות: מבנה דפים, מובייל, ניווט 44px, שאלות פתוחות, בגרות, משחקים, תמונות, מפה, מצגת, התקדמות, בוט, כיתה פלוס, דשבורד מורה, עריכה, נגישות, ביצועים ורשימת בדיקות לפני פרסום.
- נקבעה הגדרה מפורשת ל״יחידה גמורה״ והבחנה בין יחידה פעילה לבין יחידה שממתינה להשלמת מדיה מאושרת.
- כל AI שממשיך את הפרויקט צריך לקרוא מסמך זה לפני בנייה או שינוי של יחידה.

## 2026-08-24 — Claude Code — תיקון שליחת תשובות פתוחות בעמקים, ים המלח והגליל

- **בעיה שנמצאה:** בבדיקה מול הבקאנד האמיתי (bagrut.gs) התברר ששלוש מתוך חמש היחידות (`valleys-unit.js`, `dead-sea-unit.js`, `galilee-unit.js`) שולחות `submitOpenAnswer` כ-GET עם `URLSearchParams`, בעוד השרת (`doPost`) מצפה ל-POST עם גוף JSON — בדיוק כפי שכבר ממומש נכון ב-`unit.js` וב-`region-unit.js`. בפועל הבדיקה מול הבוט והשמירה בגיליון לא קרו כלל ביחידות האלה, אך הממשק הציג הודעת הצלחה גנרית — הפרה של הכלל המפורש בסעיף 14 של `docs/UNIT_STANDARD_SPEC_HE.md`: "אין להציג הצלחת שמירה אם השרת לא אישר אותה".
- **בעיה נוספת:** `valleys-unit.js` בדק זיהוי התחברות מול מפתח `sessionStorage` שגוי (`kitaPlusUser`) בלי fallback למפתח האמיתי `kitahUser` — כך שהוא מעולם לא זיהה תלמיד/ה מחובר/ת, גם כשההתחברות תקינה. `dead-sea-unit.js`/`galilee-unit.js` כבר כללו fallback ל-`kitahUser`, אז שם רק תוקן פורמט הבקשה.
- **תוקן:** שלושת הקבצים עודכנו לשלוח `fetch(API,{method:'POST',body:JSON.stringify({action:'submitOpenAnswer',token,...})})`, לבדוק `sessionStorage.kitahUser` בלבד, ולבדוק `data.ok` לפני הצגת משוב — תואם בדיוק לדפוס העובד ב-`unit.js`/`region-unit.js`. עיצוב ותוכן לא שונו.
- **נבדק:** `node --check` עבר על שלושת הקבצים. לא בוצעה בדיקה חיה בדפדפן עם משתמש מחובר אמיתי — מומלץ לוודא ידנית בפעם הראשונה שתלמיד/ה כותב/ת תשובה פתוחה בכל אחת מהיחידות האלה.
- **פתוח (לא בטיפול הזה):** `galilee-unit.js` שולחת `unit_id:'galil'` שאינו רשום ב-`BAGRUT_UNITS` בבקאנד — התשובות יישמרו ב-`open_answers` ויוצגו למורה, אך לא יופיעו כהתקדמות/ציון ביחידה בדשבורד (כמו בזמנו "העמקים"/"ים המלח" לפני מסמך 03 בתיקיית התיאום). דורש תוספת דומה ל-03 אם רוצים מעקב מלא לגליל.
