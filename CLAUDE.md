# CLAUDE.md — מצב נוכחי, tourism-bagrut-v2

מסמך זה הוא מקור-האמת למצב **הנוכחי** של הפרויקט — לא היסטוריה (זה תפקיד
`AI_WORKLOG.md`) ולא חזון (זה תפקיד המסמכים ב-`docs/`). כל סשן Claude חדש
שעובד על הריפו הזה צריך לקרוא את הקובץ הזה **קודם כל**.

## מפת המסמכים — מה לקרוא ומתי

- **הקובץ הזה** — המצב הנוכחי, החלטות ארכיטקטורה, מוסכמות קוד, סיכונים ידועים.
  קרא ראשון, תמיד.
- `TASKS.md` — תיבת דואר חיה למשימות פתוחות ל-B/קודקס (לא היסטוריה).
  אם המשתמש שולח אותך לבדוק אותה — קרא רק את הבלוק המיועד לך.
- `docs/AI_TOOLS_CAPABILITIES.md` — מחקר עומק (מבוסס חיפוש חי, 27.08.2026)
  על יכולות קלוד/GPT/קודקס/ג'מיני/NotebookLM ומתי להשתמש בכל אחד, כולל
  טבלת האצלה מעשית ורעיונות מבוססי-יכולת (פודקאסטים, תמונות דקורטיביות
  וכו'). קרא לפני שמחליטים להוסיף כלי חדש לצנרת. **הערה חשובה:** לפי
  "מדיניות האצלה" למעלה — לא לבנות את הצנרת המורחבת הזו לפני שמדיניות
  קודקס (עיצוב בלבד, בלי לגעת בריפו) הוכיחה את עצמה על כמה סבבים אמיתיים.
- `docs/UNIT_STANDARD_SPEC_HE.md` — התקן הפדגוגי/UX המחייב לכל יחידה (מבנה
  דפים, מובייל, נגישות, הגדרת "יחידה גמורה"). קרא לפני בניית/עריכת תוכן.
- `docs/UNIT_EXPANSION_PLAYBOOK.md` — התהליך הטכני המחייב להוספת יחידה חדשה
  + התוכנית ארוכת-הטווח (חבלים עתידיים, תמונות, נושאים לא-גיאוגרפיים) +
  היסטוריית ההחלטה על מודל הארכיטקטורה (סעיף 9 — "החלטת המועצה"). קרא לפני
  שמתחילים יחידה חדשה או משנים קובץ משותף.
- `tourism11/CONTENT_GUIDE.md` (ריפו אחר!) — **מקור התוכן הפדגוגי המחולץ**:
  כל 84 מושגי הבגרות מסווגים ל-6 אשכולות, מפרט מלא לכל חבל. כשבונים יחידת
  "מושגים" או צריך לוודא נוסח מושג — קוראים משם, לא ממציאים מחדש.
- `docs/PRODUCT_VISION.md` / `docs/ARCHITECTURE.md` — מסמכי חזון **מוקדמים**
  (מלפני שהאתר נבנה בפועל). לא סותרים את המצב הנוכחי בגדול, אבל
  `ARCHITECTURE.md` מכיל סעיף "מודל תוכן מתוכנן" (`course`/`unit`/`section`/
  `content_block`/...) **שמעולם לא נבנה ולא ייבנה בטווח הנראה לעין** — ר'
  "החלטת הארכיטקטורה" למטה. אל תניחו שהמודל הזה קיים.
- `docs/NEW_REGION_ONE_SHOT_PROMPT.md` — **פרומפט מוכן להעתקה** להוספת
  חבל חדש בסשן אחד: ממלאים שדות, מדביקים לסשן חדש עם תיקיית דרייב מצורפת.
  קרא/עדכן את זה כשמוסיפים חבל, לא כותבים מפרט מחדש בכל פעם.
- `docs/GALILEE_SOURCE_MAP.md` — מיפוי מקורות ספציפי ליחידת הגליל.
- `docs/YEAR_PLAN_2026_27.md` — תכנית הוראה שנתית (לא קשור לארכיטקטורת הקוד).
- `AI_WORKLOG.md` — יומן היסטורי מלא, כל שינוי מתועד שם עם תאריך. לחפש שם
  כשצריך "למה זה כך" או "מתי זה נעשה" — לא כדי להבין את המצב הנוכחי.

## מדיניות האצלה לקודקס (27.08.2026, עודכן 27.08.2026)

**קודקס אסור לו לגעת בקוד/בריפו בכלל — לא commit, לא push, לא עריכת קובץ
ישירות.** תפקידו רק לייצר **תוצרים לביקורת**: הצעות עיצוב/CSS, מוקאפים,
תרשימים, טקסט — כטקסט/תמונה שהוא מחזיר למשתמש (או ל-Claude), לא כשינוי
בריפו. **האינטגרציה לקוד תמיד דרך Claude/B**, אחרי ביקורת — כדי שהתוצאה
תהיה מחוברת נכון ולא "טלאים" (עיקרון מפורש מהמשתמש, 27.08).
הסיבה שמדיניות קודמת (commit ישיר של קודקס לתיקון ממוקד) בוטלה: אותו
כלל בדיוק שגרם לכך ש"ביקורת מועצה" (23.08, `UNIT_EXPANSION_PLAYBOOK.md`
§9) נעקפה בשקט — שני סוכנים עורכים את אותו ריפו בלי גייט מרכזי אחד.
כשעולה משימה שמתאימה לחוזק של קודקס (עיצוב/CSS/פוליש חזותי, לא לוגיקה
או בקאנד) — להוציא למשתמש פרומפט מוכן ומוגדר-היקף **שמבקש תוצר להעברה
חזרה, לא commit**, דרך `TASKS.md` (ר' למטה) או ישירות בצ'אט. אחרי
שהתוצר חוזר — Claude מבצע ביקורת (השוואה למקור, בדיקת עקביות) ומשלב
בעצמו לקוד, ומתעד ב-`AI_WORKLOG.md`.

**כלל חובה לכל סבב קודקס (מסקנת מועצת-יועצים, 27.08.2026):** לרשום שורה
אחת קבועה ב-`AI_WORKLOG.md` בפורמט `התקבל מקודקס ב-<תאריך>, נבדק ע"י
Claude, שולב כ-commit <hash>` — גם אם השילוב טריוויאלי. זה המנגנון הזול
שהיה חסר לפני "ביקורת מועצה" שנעקפה בשקט; המטרה שכל שילוב יהיה עקבה
מפורשת, לא רק זיכרון-שיחה.

**לפני הרחבה לכלים נוספים (NotebookLM/Gemini וכו', ר' `docs/AI_TOOLS_
CAPABILITIES.md`):** לא להוסיף עד שהמדיניות הזו (קודקס בלבד) עברה כמה
סבבים אמיתיים בלי "עקיפה בשקט". זו לא זהירות סתמית — זו מסקנה מפורשת
של מועצת-יועצים (5 זוויות, ביקורת עמיתים) שהמורה ביקש להפעיל: הצוואר-
בקבוק האמיתי הוא קיבולת-הביקורת של Claude/המשתמש, לא מחסור בכלים. אם
מוסיפים בכל זאת — כשלב מנוסה-מוגבל אחד (כלי אחד, יחידה אחת), לא ארבעתם
יחד.

**עדכון (27.08.2026, אחרי דיאלוג עם המשתמש) — שני מהחששות המקוריים נחלשו:**
1. **"עומס ידני על המורה"** — התברר שגוי: ל-B יש שליטת דפדפן אמיתית (כבר
   מוכחת בפרויקט הזה — עורך Apps Script, GitHub) ולפי המשתמש גם לקודקס.
   NotebookLM אין לו API, אבל זה לא אומר שצריך תפעול ידני — B יכול
   פוטנציאלית להפעיל את הממשק בעצמו (העלאת קבצים, לחיצה על Studio panel)
   אם ניתנת לו גישה לחשבון. **לא נבדק בפועל עדיין** — ר' §6 ב-
   `AI_TOOLS_CAPABILITIES.md`.
2. **"סיכון דיוק/הזיה בתוכן NotebookLM"** — המשתמש קיבל את הסיכון הזה
   במפורש ("השתמשתי בזה בעבר ואף פעם לא היו תקלות") — זו החלטה מודעת
   שלו, לא פער שפספסנו.

**מה עדיין עומד:** חשש-הליבה של המועצה — ריבוי "כותבים"/תהליכים על
אותו ריפו בלי גייט מרכזי — לא טופל על ידי אף אחד מהשניים הנ"ל. **המסקנה
המעשית:** פיילוט מוגבל אחד (NotebookLM דרך B, על יחידת אילת שמגיעה
בקרוב) הוא בדיוק ה"שלב מנוסה-מוגבל" שהמועצה המליצה עליו — לא הרחבה
מלאה לארבעת הכלים.

## החלטת הארכיטקטורה (25.08.2026)

ב-23.08 "ביקורת מועצה" (מתועדת ב-`UNIT_EXPANSION_PLAYBOOK.md` §9) קבעה
שאין לשכפל את מישור החוף כפי שהוא לפני חילוץ מודל-נתונים מלא + מנוע תצוגה
אחד (P0 כבד: `sourceId`/גרסה/אישור לכל פריט, טיוטה→פרסום, וכו'). בפועל, זה
**לא בוצע** — העבודה המשיכה בכיוון קל יותר: קובץ-התנהגות משותף
(`region-unit.js`-family) לארבע היחידות, בלי חילוץ למודל נתונים.

**ההחלטה הרשמית עכשיו: לאשרר את הדפוס הקל כתקן, לא לבצע את ה-P0 הכבד.**
הסיבה: רוב יתרון היציבות במחיר נמוך משמעותית, וכבר מאומת בפועל על 4
יחידות. הפער היחיד שנשאר פתוח הוא **מישור החוף עצמו**, שמעולם לא אוחד
לדפוס הזה (ר' "מצב לפי יחידה" למטה).

## מנוע אחד לכל היחידות (04.09.2026)

**4 מ-5 היחידות (ירושלים, העמקים, ים המלח, הגליל) רצות על `unit-runtime.js` אחד** לפי החוזה
ב-`docs/UNIT_TEMPLATE.md`. הקבצים `region-unit.js`, `valleys-unit.js`, `dead-sea-unit.js`,
`galilee-unit.js` נמחקו. התוכן של כל יחידה = HTML סטטי + `#unitData` (שקופיות, בוחן, בגרות).
הבוחנים (20 שאלות לחבל) הועברו מ-`tourism11` כמו שהם. **מישור החוף עדיין על `unit.js`**
(המשחקים שלו קשיחים) ומקבל רק את המעטפת המשותפת; העברתו למנוע היא המשימה הפתוחה הגדולה.
אכיפה: `npm test` = check + standard + smoke + behavior + teacher (דשבורד המורה מול שרת מדומה, `tests/teacher.mjs`). סעיפי "מצב לפי יחידה" למטה מתארים
את המצב שלפני האיחוד ונשארים כהיסטוריה עד לעדכון הבא של הקובץ.

## התקן לכל יחידה (מה שכל יחידה חדשה חייבת לעמוד בו)

1. דף HTML אחד ב-`units/<slug>.html`, תוכן **סטטי בתוך ה-HTML** (לא מוטבע
   כמחרוזות JS), מחולק לדפי-משנה עם `data-page-panel="<id>"`.
2. **אין קובץ JS ליחידה.** כל יחידה נטענת עם `unit-runtime.js` המשותף לפי החוזה ב-
   `docs/UNIT_TEMPLATE.md` (`data-unit-*` על `<body>`, `#unitData` עם שקופיות/בוחן/בגרות).
   מעתיקים את `units/valleys.html` כתבנית. (עד 04.09 היה כאן `<slug>-unit.js` לכל יחידה; הקבצים נמחקו.)
3. **`data-field-key` על כל טקסט מהיום הראשון** (לא בדיעבד) — פורמט:
   `<unit_id>__<page>__<tag><n>` (למשל `haamakim__overview__p1`). זה מה
   שמפעיל אוטומטית את עריכת-התוכן למורה (`content-overrides.js` +
   טאב "עריכת תוכן" ב-`teacher.html`) — בלי עבודה נוספת אם המפתחות קיימים.
   **אזהרה:** המפתחות ממוספרים לפי סדר במסמך. שינוי מבני בדף קיים (הוספה/
   הסרה של פסקה) מזיז את המספור בשקט — תשובה שמורה תחת `p3` עלולה "לנחות"
   על פסקה אחרת בלי שגיאה. אחרי כל שינוי מבנה בדף קיים עם תוכן ערוך —
   לבדוק את הערכים השמורים, לא רק להוסיף.
4. ווידג'ט "המאמן שלי לבגרות" — מעתיקים את ה-markup + הפונקציות
   (`callBagrutBot`/`toggleCoachWidget`/`appendCoachMsg`/`sendCoachQuestion`)
   בדיוק כפי שמופיעות ב-4 היחידות הקיימות. לא דורש פריסת בקאנד — `askBagrutBot`
   כבר פתוח וחי.
5. **מינימום לשיגור:** תוכן + תמונות (אם יש, רק מ-jerusalem-tour) + שאלה
   פתוחה בכל דף + תרגול/בוחן. **משחקים עשירים (זיכרון/פאזל/ציר-זמן) הם
   העשרה מאוחרת, לא תנאי לפרסום** — כדי שתוכן חדש לא ייתקע בגלל בניית משחקים.
6. עדכון גרסת cache-busting (`?v=N`) בכל קובץ ששונה, בכל דף שטוען אותו.

## מצב לפי יחידה (04.09.2026)

| יחידה | unit_id | קוד | דפים | בוחן | תמונות | הערות |
|---|---|---|---|---|---|---|
| מישור החוף | `mishor_hachof` | **`unit.js` (ישן)** | 10 | משלו | כן | 10 משחקים קשיחים ב-JS. מקבל את המעטפת המשותפת בלבד. להעברה למנוע כשיש דפדפן לאימות |
| ירושלים | `yerushalayim` | `unit-runtime.js` | 8 | 20 (tourism11) | כן + מפה אילמת | |
| העמקים | `haamakim` | `unit-runtime.js` | 7 | 20 (tourism11) | כן | |
| ים המלח | `yam_hamelach` | `unit-runtime.js` | 7 | 20 (tourism11) | כן | |
| הגליל | `galil` | `unit-runtime.js` | 8 | **אין** (ממתין לאישור) | **אין** (ממתין למאגר) | טיוטת בוחן לאישור: `docs/drafts/galil-quiz-draft.json` |

כל היחידות דורשות כניסה דרך כיתה פלוס. `npm test` אוכף את התקן (`docs/UNIT_TEMPLATE.md`).

## בקאנד — הקוד בריפו, תחת `backend/` (מ-04.09.2026)

**המקור היחיד של הבקאנד הוא `backend/` בריפו הזה.** אין לערוך בעורך ה-Apps Script ישירות.
commit ל-`main` שנוגע ב-`backend/` מפעיל את `.github/workflows/deploy-backend.yml`,
שמפרסם גרסה חדשה על ה-deployment הקיים (אותה כתובת API). פרטים ב-`backend/README.md`.
המשמעות: "מה פרוס" = הקומיט האחרון ב-`main` שנגע ב-`backend/`, וזה מופיע בתיאור
הגרסה ב-Manage deployments. סעיף "דריפט תיעוד" בסיכונים למטה נסגר בזכות זה.

**לשיעור עצמו:** `docs/FIRST_LESSON_CHECKLIST.md` (לפני/במהלך/אחרי, ומה לעשות אם משהו נשבר).

**בדיקת בריאות יומית:** `.github/workflows/health-check.yml` רץ כל בוקר לימודים (07:00, א׳–ה׳)
ובלחיצה ידנית: מוודא ששני האתרים החיים עונים 200, ש-`doGet` וה-API עונים `ok:true`, ושפעולה
מוגנת בלי טוקן נדחית ב-JSON מסודר. כשל = מייל מ-GitHub. הבדיקה רק קוראת, לא משנה כלום.

**אימות טוקן עם מטמון (E11):** `verifyGoogleToken` זוכר טוקן שאומת ב-CacheService עד 10 דקות
(ולעולם לא אחרי `exp` של הטוקן). רק הצלחות נשמרות; אם המטמון לא זמין, מאמתים מול Google כרגיל.

## בקאנד — משותף לגמרי עם tourism11

**אין "בקאנד ישן" מול "בקאנד חדש" — זה אותו `bagrut.gs`/`code.gs` בדיוק**,
אותו deployment
(`AKfycbwf3-MNZBBi64zXcNH7wfhBRoEBl9brtQ9QRI4Won5RmUIOrl_WBivN6uI5NAp6Mc0h`).
כל שינוי שם משפיע על שני האתרים.

### פעולות פרוסות וחיות
`askBagrutBot` (פתוח, בלי login; מ-01.09.2026 עם retry יחיד + הודעת
נפילה בעברית אם `callGemini` נכשל — **לא** מדליף שגיאת API גולמית
לתלמיד. `submitOpenAnswer` קוראת ל-`callGemini` גם היא, בלי אותה הגנה —
מועמד לתיקון דומה), `submitOpenAnswer` (עם בדיקת מורה +
`pending_review`/הגנת prompt-injection, מ-25.08), `getBagrutStudentOpenAnswers`,
`getBagrutTeacherDashboard`, `addBagrutStudent`, `removeBagrutStudent`,
`saveBagrutQuizResult`, `reviewOpenAnswer`, `getBagrutPendingReviewsForTeacher`,
`getMyReviewNotices`, `ackReviewNotice`, `getContentOverrides` (פתוח),
`getAllContentOverrides`, `saveContentOverride`, `getBagrutUnits_` (פנימי,
לא action), `addBagrutUnit` (מוגן), `addBagrutStudentsBulk` (05),
`updateBagrutMistakes`/`getBagrutMistakes`/`getBagrutMistakesSummary` (06),
`getMySiteRecognition`/`saveSiteKnown`/`resetMySiteRecognition`/
`getBagrutSiteRecognitionSummary` (07), `submitPageFeedback`/`logClientError`
(08, פתוחות בכוונה — לא ב-protectedActions). כל 05-08 פרוסות מ-26.08.2026,
גרסה 66.

### `BAGRUT_UNITS` — קבוע בקוד (אומת 04.09.2026)
**התיעוד הקודם כאן ("הפך לדינמי, `getBagrutUnits_` קורא מטאב `bagrut_units`, פעולה `addBagrutUnit`")
לא תאם את הקוד.** המקור שיוצא מהעורך ב-04.09 (ועכשיו יושב ב-`backend/bagrut.gs`) מכיל `BAGRUT_UNITS`
קבוע ואין בו `getBagrutUnits_` או `addBagrutUnit`. יחידה חדשה נרשמת בעריכת המערך ב-`backend/bagrut.gs`
(commit ל-main = פריסה). אין טאב `bagrut_units` בשימוש.

## סיכונים/מוסכמות שחשוב לזכור

1. **אין בדיקה אוטומטית מעבר ל-`node --check`** (תחביר בלבד). שינוי בקובץ
   משותף (`app.js`, `content-overrides.js`, `region-unit.js`) = לבדוק ידנית
   בכל היחידות שמשתמשות בו, לא רק באחת.
2. **דריפט תיעוד מול מציאות** — אין דרך לאמת מרחוק שמה שמתועד כ"פרוס" באמת
   פרוס בקאנד (אין גישת Apps Script ישירה). מקור האמת הוא בדיקה בפועל דרך
   ה-frontend, לא התיעוד.
3. **פורמט נתונים למשחקים/בוחנים — הוחלט (25.08), טרם בוצע.** נקראו כל 10
   המשחקים הקיימים במלואם (`unit.js`) והם מתכנסים ל-7 "מנועי משחק" גנריים:

   | מנוע | דוגמאות שימוש קיימות | דאטה נדרשת |
   |---|---|---|
   | `select-match` | אזורים, מושגים | `[{label, correct}]` + מאגר תשובות |
   | `multi-choice-sequence` | רמזים, אתגר מהיר | `[{prompt, options[], correct, explain}]` (+טיימר אופציונלי) |
   | `ordered-select` | מסלול | `options[]` + `correctOrder[]` |
   | `memory-pairs` | זיכרון | `[[מונח, עובדה], ...]` |
   | `image-puzzle` | פאזל | `{image, grid:3x3}` |
   | `map-points` | מפה, מפה אילמת | `[{name, top%, left%, note}]` (+ מצב לחיצה-לזיהוי/גרירה-למיקום) |
   | `streak-quiz` | רצף נכון/לא נכון | `[{statement, answer:bool, explain}]` + יעד-רצף |

   כשבונים משחק ליחידה חדשה — לזהות איזה מנוע מתאים ולהזין דאטה בפורמט
   הזה, לא לכתוב לוגיקה חדשה מאפס (וגם לא לחזור על JS קשיח כמו מישור החוף).

   **חילוץ מישור החוף עצמו לפורמט הזה עדיין לא בוצע — ובכוונה.** `unit.js`
   (268 שורות, כל 10 המשחקים + דאטה + לוגיקה מעורבבים) הוא פיצ'ר עשיר
   שכבר עובד בפועל לתלמידים. **אסור לבצע את החילוץ הזה "בעיוורון"** (בלי
   גישת דפדפן לבדוק ויזואלית שכלום לא נשבר) — הסיכון גבוה מדי ביחס לתועלת
   כשאין דרך לאמת. לבצע רק בסשן עם גישת דפדפן אמיתית (כמו B), או בשלבים
   קטנים עם אימות אנושי אחרי כל שלב.
4. **תמונות אך ורק מ-`jerusalem-tour`** — אף פעם לא ממקור חיצוני, אף פעם לא
   מחבל אחר. פרסום תמונות אמיתיות של נסים דורש אישור מפורש שלו בכל אצווה,
   לא אישור גורף.
5. **מצב אורח (guest) — הוסר לגמרי (25.08.2026).** כל האתר דורש כעת כניסה
   דרך כיתה פלוס (`sessionStorage.kitahUser`) — `index.html` וכל 5 דפי
   היחידות עטופים ב-gate כושל-בטוח (`.protected-page`/`.access-gate`,
   אותו דפוס כמו `teacher.html`). הקוד המת שקישר ל-guest (דיאלוג login,
   כפתור "כניסה" בטופבר, הערת guest-note בסיידבר, `data-login-feature`)
   הוסר. אין fallback ל-localStorage יותר.
6. **"זיהוי אתרים" (חידון תמונות) ו"מה שכחתי" — קיימים רק במישור החוף.**
   אומת ב-26.08 ע"י B: אין מדובר בבאג בחיווט — התוכן/המסך פשוט לא נבנו
   עדיין ביתר 4 היחידות, ואין עדיין מסך סיכום "מה שכחתי" בכלל (רק ה-API
   הקיים). אם רוצים את זה בכל היחידות — זה scope חדש, לא תיקון.
7. **חוב אימות-דפדפן — ברובו נסגר (27.08.2026), לקח לזכור.** שני סבבי
   בדיקה חיה (26-27.08, ר' AI_WORKLOG) מצאו ותיקנו **3 באגים אמיתיים**
   שלא היו נתפסים בלי דפדפן: coach widget שנשבר ויזואלית (CSS),
   `saveSiteKnown` שלא נשלח בכלל, ו-**המאמן שהיה שבור לגמרי** (`API is
   not defined`) ב-3 מתוך 5 יחידות בגלל scope שגוי של משתנה בתוך
   closure. שער הכניסה בכל 6 הכתובות וייבוא ה-CSV אומתו קצה-לקצה ועברו.
   **המסקנה הקבועה:** שינוי בקובץ `*-unit.js` שמוסיף קוד **מחוץ** ל-IIFE
   הקיים (כמו ווידג'ט המאמן) חייב לבדוק שמשתנים כמו `API` נגישים משם —
   או לחשוף אותם כ-`window.X` מראש. תועד גם ב-
   `docs/NEW_REGION_ONE_SHOT_PROMPT.md` כאזהרה מפורשת ליחידות עתידיות.
   **נשאר פתוח:** למחוק ידנית תלמיד-בדיקה (`qa.test.student@example.com`)
   שנוצר בבדיקת ה-CSV — לא הוסר אוטומטית.
