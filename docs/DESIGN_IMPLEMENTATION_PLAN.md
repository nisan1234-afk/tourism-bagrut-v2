# תוכנית הטמעת העיצוב של הדיזיין (התקבל 15.09.2026, 21:28)

המקור: 36 צילומי מסך (18 מסכים, מחשב + נייד) ב-`docs/design-mockups/`. אין קובץ משתנים או CSS — רק תמונות.
העיצוב הוא ליטוש של המבנה הקיים באותם צבעים (ירוק כהה, זהב, קרם) ובאותם רכיבים מחבילת המסירה. לא מבנה חדש.

**כלל:** מטמיעים בענף `claude/design`, לא נוגעים ב-`main` לפני שיעור ירושלים (16.09). `npm test` חייב להישאר ירוק;
המנוע `unit-runtime.js` לא משתנה אלא אם חובה. תמונות רק מהמאגר הרשמי (`assets/official/`).

## מה השתנה בכל מסך (מהמוקאפים)

1. **דף הבית (01):** הירו עם תמונה רשמית בכרטיס + שבב התקדמות; כרטיסי היחידות עם תמונת כיסוי רשמית במקום גרדיאנט,
   מספר בעיגול זהב, שבבי מטא, "המשימה להיום" ככרטיס ירוק כהה; פוטר קטן. בנייד: כרטיסים בטור אחד.
2. **ההתקדמות שלי (02):** 4 אריחי מספרים למעלה, כרטיס לכל יחידה עם שורות "— ", כרטיס "מה שכחתי" בקרם.
3. **מבוא יחידה (03/14):** כרטיס המאמן (קיים) + כרטיס "בסיום היחידה תוכלו:" (חדש, רשימה בתוך כרטיס ירוק);
   `.hero-media` עם תמונת פוסטר רשמית, כפתור נגן זהב וכיתוב ("ירושלים ב-3 דקות · NotebookLM"); כותרות מקטע ממוספרות
   ("01 עיר אחת, שלוש דתות") עם eyebrow; המאמן נפתח כחלון בפינה (קיים).
4. **דף תוכן (05/06):** שורת 3 תמונות רשמיות עם שם + שורת קרדיט "מתוך חומרי הגלם והמצגות של אתר תיירות אלוני הבשן";
   כרטיסי תוכן ממוספרים 01/02; כרטיס ציטוט ירוק ("לצורך מה מושכת חיפה?"); שאלת סיום עם רשימת רכיבים ✓/○ (קיים);
   משחקים בפאנל ירוק בהיר "בודקים הבנה לפני שממשיכים" (קיים).
5. **מפה/רמזים/מפה אילמת (06), זיכרון/מהיר (07):** כמו היום, ריווח ושבבי "משחק N".
6. **זיהוי אתרים (08/09):** רשת 3×2 של פוסטרים עם שבב "מהו האתר?" / "✓ זוהה"; משחק זיהוי בכרטיס עם תמונה מימין
   ואפשרויות משמאל; מפה אילמת בכרטיס נפרד.
7. **מצגת (10):** שקופית תמונה שמאל / טקסט ימין על רקע ירוק, נקודות התקדמות, כפתורים בתוך הכרטיס.
8. **בוחן (11/12):** קווי התקדמות קטנים, אפשרויות עם ✓/✗ צבעוניים, משוב באדום/ירוק, "לשאלה הבאה".
9. **מאגר בגרות (13):** פס התקדמות "2 מתוך 9 שאלות הושלמו", כרטיס שאלה עם שבב נושא, ירוק כשעברה.
10. **דשבורד המורה (15–19):** טאבים כגלולות, כרטיס "מה דורש את תשומת הלב" ירוק עם CTA זהב, 4 אריחי מספרים, דוח שיעור.

## סדר עבודה מומלץ (השפעה גבוהה, סיכון נמוך קודם)

1. `styles.css`: כרטיסי יחידה עם תמונת כיסוי רשמית (`.unit-cover` עם `<img>` + שכבה כהה), מספר בעיגול זהב; הירו בדף הבית.
2. יחידות: כותרות מקטע ממוספרות (`.block-heading` כבר קיים לדף המאגר — להרחיב לכל דף), כרטיס "בסיום היחידה תוכלו" בדף 1
   (HTML סטטי בכל יחידה, `data-field-key`), `.hero-media` עם פוסטר + כפתור נגן (הסרטונים עצמם עדיין לא הגיעו).
3. שורת קרדיט מתחת לתמונות, ליטוש בוחן/מאגר/מצגת לפי 10–13.
4. דשבורד המורה (15–19) — אחרון; לא נוגע בלוגיקה של `teacher.js`.
5. אחרי כל שלב: `npm test` + צילומי מסך (סקריפט `shots2.mjs` בסקראצ'פד, להעתיק לריפו זמנית) והשוואה למוקאפ.

## מה חסר מהדיזיין
- קובץ CSS/משתנים (עובדים מהתמונות), הסרטונים ל-`.hero-media`, תמונות גליל/גולן/נגב (ממתין ל-ZIP מפוצל מנסים).

## סטטוס 16.09 (03:30–04:00) — ענף `claude/design`, לא ב-main

בוצע (CSS בסוף `styles.css` תחת "רענון עיצוב 16.09", HTML מינימלי, שתי תוספות קטנות במנוע):
- דף הבית: הירו עם תמונה רשמית (ראש הנקרה) ושבב התקדמות; כרטיסי יחידה עם תמונת כיסוי רשמית (4 יחידות), מספר בעיגול זהב,
  שבבי מטא, כפתור מלא. גליל/גולן/נגב נשארו בגרדיאנט עד שיגיעו תמונות.
- יחידות: עיגולי מצב במסילה (המספור הוסר מהטקסט ועבר לעיגול), מספר דף גדול בזהב ליד הכותרת (`unit-runtime.js` מוסיף
  `.page-number`), כרטיס "בסיום היחידה תוכלו" ב-5 יחידות (ירושלים ומישור החוף כבר היו להן בלוקים משלהן), כיתוב על תמונת
  הפתיחה, דף תמונות ברשת 3×2 עם "מהו האתר?"/"✓ זוהה" ושורת קרדיט, בוחן עם קווי התקדמות ו-✓/✕, שבב נושא במאגר, מצגת
  תמונה-לצד-טקסט (שקופיות העמקים וים המלח הועברו לקבצים המקומיים).
- `npm test` ירוק (standard, smoke, behavior ל-7 יחידות, teacher). צילומי מסך: מחשב ונייד.

נשאר: דשבורד המורה (15–19, ליטוש קטן), כפתור נגן ב-`.hero-media` כשיגיעו סרטונים, תמונות גליל/גולן/נגב.
